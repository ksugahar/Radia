# examples/ Consolidation Plan -- 2026-06-26

READ-ONLY inventory synthesis. No files were edited or deleted producing this
plan. Goal (user directive): promote worth-keeping example demonstrations to
TRACKED `docs/<topic>/*.ipynb` notebooks (the `docs/axifem/*.ipynb` model),
then delete the promoted / obsolete `.py` from `examples/`. The work is
multi-session and DESTRUCTIVE, so deletions follow the phased order below and
NEVER precede reference migration.

Generated by the `examples-consolidation-inventory` workflow (6 cluster readers
+ synthesis). Each promotion is a 3-fold op (user directive): (1) author the
tracked `docs/<topic>/*.ipynb` bundling code + committed JSON + saved results,
(2) strengthen the `radia_mcp.<domain>` knowledge (re-home example references at
the new notebook AND enrich with validated insights), (3) verify references
migrated, then delete the source `.py`.

## Executive Summary

The `examples/` tree holds ~2385 tracked files across 42 inventoried topics in
5 clusters. By topic count: 8 promote, 19 keep-example, 9 mixed, 5 prune, 1
blocked. By FILE volume the tree is dominated by keep-example + blocked: the
blocked `maglev/research_cln` is 435 files; the registered cross-validation
corpus (`cubit_mesh_export`, `build123d_netgen_gmsh_flow`, `rf_waveguide`,
`acoustic_bem`, `electric_machine`, `electrostatics`, `fem_readable`,
`optimization`) is ~280 files; and the application tiers (`stream_function`,
`vim`, `peec_integration`, `ih_esim_benchmark`, `induction_heating`) add
hundreds more.

Biggest CLEAN promote wins: small zero-reference topics (`clebsch_legendre`,
`smco_magnet_array`, `mesh_fusion`, `bem_extractor`). Biggest prune target:
`kelvin_transformation/AdaptiveMesh` (76 convergence-matrix files + committed
`.vtu`) and the ~18 redundant `solver_benchmarks/benchmark_*.py`.

TWO HARD CI LOCKS gate the destructive pass:
1. `packages/radia-mcp/tests/test_cross_validation_registry.py` HARD-ASSERTS
   literal strings like `examples/cubit_mesh_export/validation_vol_*_summary.json`,
   `examples/build123d_netgen_gmsh_flow/validation_*_summary.json`,
   `examples/rf_waveguide/validation_*_summary.json`,
   `examples/electric_machine/validation_*_summary.json`,
   `examples/acoustic_bem/validation_*_summary.json` (verified). Deleting any of
   those `validation_*_summary.json` artifacts breaks CI.
2. The shipped `urn_fit_from_csv` MCP tool RUNTIME-imports
   `examples/universal_relaxation_network/universal_relaxation_network.py` +
   `relaxation_basis_library.py` via a sys.path walk-up (verified urn.py
   L185-201). Those core files must NOT be deleted without re-homing the impl.

CONFIRMED LATENT BUG (fix before any maglev deletion): `tests/test_team28_cln_golden.py`
references `examples/maglev/team28/` in its docstring but the inventory reported
a stale `examples/levitation/team28` resolution -- `examples/levitation/` does
NOT exist; the actual dir is `examples/maglev/team28`. Verify and correct the
test path; it is also a latent CI hole (the golden may silently skip).

## Per-Topic Plan

| Topic | Files | Bucket | promote_what | References | Risk | Action |
|---|---|---|---|---|---|---|
| maglev (top-level scripts) | 17 | keep-example | curated radia.levitation example tier (cube_alpha_sweep, physical_tensor_rom) | 2 goldens + maglev knowledge | med | KEEP; do not move (golden-locked) |
| maglev/sphere | 12 | keep-example | validated force workflow | 3 goldens | high | KEEP (Validation-Class) |
| maglev/ellipsoid | 12 | keep-example | alpha(omega) tensor | 3 goldens + maglev knowledge | high | KEEP |
| maglev/team28 | 7 | keep-example | TEAM 28 benchmark | test_team28_cln_golden.py (STALE PATH) | high | FIX test path first; then KEEP |
| maglev/cuboid | 2 | keep-example | CLN-SIBC brick | none | low | KEEP |
| maglev/papers/igte_symposium_2026 | 10 | keep-example | LaTeX paper source | research_cln README | med | KEEP (paper tree, not a demo) |
| maglev/research_cln | 435 | blocked | DO NOT PLAN (own ipynb goal, other agent) | maglev + mor knowledge | high | BLOCKED -- never touch this pass |
| kelvin_transformation | 327 | mixed | promote ONE docs/kelvin/KELVIN_DEMOS.ipynb (A-form sphere, H-form 3D dipole, Omega-reduced sphere, radia_iem_vs_fem_sphere); prune AdaptiveMesh (76 files) | 8 knowledge/panel/wls files; DtN_spectrum is BLOCKED-adjacent | high | dedicated session; DtN_spectrum untouched |
| open_boundary | 2 | keep-example | demo for shipped radia.open_boundary | dtn_coarse_mesh knowledge | low | KEEP |
| cohomology | 2 | keep-example | tomega_wire canonical demo | homology knowledge | low | KEEP |
| equivalence_source | 12 | mixed | docs/equivalence_source/*.ipynb (phase1 + phase3 + null_field) | fem knowledge + calc_equivalence_source.py panel | med | promote then prune phase scripts AFTER ref migration |
| axifem | 38 | keep-example | already promoted (docs/axifem/*.ipynb exists) | axifem knowledge + solve dispatch + 2 tests | low | KEEP (reproducibility surface) |
| hantila_solver | 2 | keep-example | Hantila method usage | none | low | KEEP |
| eddy_current_analytical_validation | 13 | keep-example | Smythe/Dodd-Deeds analytic references | ih + sibc knowledge (by name) | med | KEEP reference scripts |
| peec_integration | 236 | mixed | docs/peec_integration/peec_overview.ipynb (ngsbem demo + PRIMA-SPICE); prune algorithm_development/analysis/verification scratch | radia.py names ngsbem 1_turn_coil + compute_L_final | high | multi-session; migrate 2 citations first |
| ih_esim_benchmark | 178 | keep-example | source-of-truth sweep JSON (Data Persistence) | test_esim_envelope + esim/panel_review knowledge | high | KEEP data; do not prune |
| induction_heating | 29 | mixed | docs/induction_heating/esim_rwg_demos.ipynb (5 README demos); prune scattered_rhs_clean_test | 6 knowledge/panel refs | med | promote then prune AFTER migration |
| effective_surface_impedance | 7 | prune | none (superseded by src esim; external-lib deps) | none | low | PRUNE (also: tracked .pkl violates Binary Policy) |
| peec_bema_convergence | 2 | keep-example | committed JSON finding (no script) | none | low | KEEP (data + README) |
| ngbem_diagnostics | 14 | prune | none (diagnose_/debug_ iteration) | none | med | distill bug lessons to memory/ then PRUNE |
| acoustic_bem | 21 | keep-example | analytic-led validation gates | registry test + ngsolve knowledge | low | KEEP (CI-locked) |
| bem_extractor | 3 | promote | docs/bem_extractor/inductance_extraction.ipynb | none (0 refs verified) | low | PROMOTE then prune 2 .py |
| vim | 153 | mixed | TWO notebooks: HDiv-VIM core + streamfunction-cohomology; prune ~70 sweep/precond/yano-study scripts; KEEP reference_yano_msc/golden | hdiv_vim + mmm_core + streamfunction knowledge, PRODUCTIONIZATION.md names 4 .py, calc_streamfunction_volume.py | high | per-file review; migrate named refs first |
| clebsch_hodograph | 119 | promote | ONE design-methodology notebook (5 backbone scripts); prune sweep corpus | 3 docs/.md + 4 MCP knowledge | high | promote then prune AFTER migrating named refs; coordinate with vim |
| clebsch_legendre | 7 | promote | docs/clebsch_legendre/*.ipynb (whole dir folds in) | none (0 refs verified) | low | PROMOTE then prune all |
| mixed_galerkin | 24 | promote | docs/mixed_galerkin/*.ipynb (cube3d + references + time_domain) | docs/README.md + cln_sibc_orthogonal knowledge | med | promote then prune redundant subdirs AFTER migration |
| hysteresis | 24 | keep-example | none (Validation-Class) | test_hysteresis.py + radia knowledge | med | KEEP (.npz goldens) |
| mmm_eigenvalue_study | 20 | mixed | optional tiny viz notebook OR memory/; prune deflation-API scratch | MSC_NULLSPACE_DEFLATION.md + mmm_core | med | lean prune; outcome already documented |
| tetra_field_accuracy_evaluation | 6 | keep-example | none (Validation-Class) | none | low | KEEP |
| stream_function | 98 | keep-example | none now (active app domain, ~15 demos named in knowledge) | streamfunction + aca_tsvd + fusion_reactor knowledge; shipped panel | high | KEEP; no deletion this pass |
| cube_uniform_field | 90 | mixed | ONE MMM/MSC solver-scaling notebook; prune benchmark result tree | radia.py (STALE: test_objm_minimal.py renamed) | med | fix stale ref; promote then prune outputs |
| universal_relaxation_network | 129 | mixed | URN-vs-VF validation notebook; KEEP core .py (RUNTIME-imported) | urn.py runtime import + server tool | high | KEEP core; prune only validate_/vf_ dev scripts |
| electric_machine | 52 | keep-example | optional cogging_skew_demo notebook | registry test + force_validation + ngsolve knowledge | high | KEEP validation pairs (CI-locked) |
| analytical_formulas | 24 | promote | docs/analytical_formulas/*.ipynb (2 cross_validation + plots) | analytical_formulas knowledge ("10 scripts" + 2 named) | med | promote then prune AFTER migration |
| force_validation | 13 | keep-example | none (Validation-Class) | force_validation knowledge (topic) | med | KEEP |
| smco_magnet_array | 3 | promote | docs notebook (smco_array.py) | none (0 refs verified) | low | PROMOTE then prune .py + .pvsm |
| nodal_force | 3 | promote | docs notebook (demo_nodal_force_2d) | ngsolve.py (1 line names file) | low | PROMOTE then prune AFTER 1-line migration |
| background_fields | 7 | promote | docs notebook (ObjBckg callback demo) | radia.py (1 line) | low | PROMOTE; prune stale .bdf/.pvsm/cubit_to_nastran regardless |
| simple_problems | 7 | promote | ONE "Radia basics" notebook (5 demos) | radia.py (1 line) | low | PROMOTE then prune AFTER 1-line migration |
| cubit_panels | 97 | mixed | KEEP accel_magnet/ (panel-sample fixtures); prune inductance/ 13 experiment_* | 5 knowledge + EM sample README | high | distill to memory; prune inductance/ AFTER migration |
| cubit_mesh_export | 73 | keep-example | none (registered validation corpus) | registry test + 3 knowledge | high | KEEP (CI-locked) |
| build123d_netgen_gmsh_flow | 199 | keep-example | none (registry + mcp-server training input) | registry test + 4 knowledge | high | KEEP (CI-locked) |
| rf_waveguide | 35 | keep-example | none (analytic-gated validation) | registry test + 2 knowledge | high | KEEP (CI-locked) |
| fem_readable | 25 | keep-example | none (readable FEM corpus) | force_validation knowledge | med | KEEP |
| ngsolve_integration | 23 | mixed | docs notebook (demos + verify_curl_A_equals_B); prune scratch | radia knowledge + CLAUDE.md + API_REFERENCE.md names verify_curl_A | med | KEEP verify_curl_A; promote demos; migrate CLAUDE.md ref first |
| electrostatics | 11 | keep-example | none (ES validation) | force_validation knowledge | med | KEEP |
| mesh_fusion | 11 | promote | docs/mesh_fusion/*.ipynb (5-phase Nitsche study) | none (0 refs verified) | low | PROMOTE then prune phase scripts |
| visualization | 8 | prune | none (README pushes REMOVED PyVista API) | gmsh_knowledge | med | verify gmsh ref; distill GmshPostExport demo; PRUNE |
| jou_translation_bench | 8 | prune | none (meta-benchmark harness) | none | low | distill verdict to memory; PRUNE |
| gmsh_animation | 18 | prune | marginal (moving_body_demo) | none | low | PRUNE regenerable outputs (verify .step/.msh regen first per policy) |
| optimization | 5 | keep-example | none (validation corpus spirit) | ngsolve knowledge (registry) | med | KEEP |
| complex_coil_geometry | 5 | keep-example | none (canonical CoilBuilder example) | docs/api/CoilBuilder.md + src README | med | KEEP; prune only .pvsm |
| figures | 4 | keep-example | none (TikZ assets) | none | low | KEEP |
| hiruma_xfem_comparison | 14 | promote | docs/hiruma_xfem/*.ipynb (4-phase XFEM study) | 3 fem knowledge files (topic name) | med | promote then prune AFTER migration |
| mathematica | 2 | keep-example | none (RadiaBasis.m reference) | basis_functions knowledge + test_basis_functions.py | med | KEEP (separate .wls refactor, not a prune) |
| mesh_generation | 2 | keep-example | none (pre-gen .msh) | none | low | KEEP (.msh preservation policy) |
| ngsolve_user_meeting | 2 | prune | none (superseded by calc_mesh_eval) | none | low | PRUNE |
| solver_benchmarks | 28 | mixed | 2 canonical notebooks (Hmatrix-scaling, PEEC-crossover); prune ~18 benchmark_* | test_peec_hacapk_smoke.py | med | verify test does not import bench_; promote 2; prune rest |

## Phased Execution Order

Each phase is its own session-bounded batch. Within every promote-then-prune
batch the rule is: (1) author the tracked `docs/<topic>/*.ipynb`, (2) migrate
every test / MCP-knowledge / panel / docs reference to point at the new
notebook, (3) ONLY THEN delete the source `.py`, (4) distill any lasting
lesson to `memory/` before deleting prune-class scratch.

### Phase 1 -- PILOT (low risk, self-contained, few/no references)

Start here to validate the promote-then-delete workflow end to end on the
safest topics. Zero-reference topics need no migration; single-reference
topics need exactly one knowledge line re-homed.

1. `clebsch_legendre` (0 refs verified) -- whole dir folds into one notebook.
2. `smco_magnet_array` (0 refs verified) -- promote smco_array.py; prune .pvsm.
3. `mesh_fusion` (0 refs verified) -- 5-phase Nitsche study -> one notebook.
4. `bem_extractor` (0 refs verified) -- 2 .py -> one notebook.
5. `nodal_force` (1 ngsolve.py line) -- migrate the line, then prune.

Also-safe prunes (no notebook needed) in the same phase, after distilling any
lesson to memory/: `ngsolve_user_meeting`, `jou_translation_bench`,
`effective_surface_impedance` (also removes a Binary-Policy-violating .pkl).

### Phase 2 -- SINGLE-REFERENCE PROMOTES

Promote then migrate one or two named-file knowledge lines:
`simple_problems`, `background_fields` (prune stale .bdf/.pvsm/cubit_to_nastran
regardless), `analytical_formulas`, `hiruma_xfem_comparison`,
`mixed_galerkin`, `ngsolve_integration` (KEEP verify_curl_A_equals_B; update
CLAUDE.md + API_REFERENCE.md only if it is ever moved).

### Phase 3 -- MIXED OUTPUT-HEAVY PRUNES (medium risk)

Promote the canonical demo(s), prune regenerable outputs / dev iteration:
`cube_uniform_field` (fix the stale `test_objm_minimal.py` knowledge ref FIRST),
`solver_benchmarks` (verify test_peec_hacapk_smoke.py imports first),
`mmm_eigenvalue_study` (outcome already in docs/solver), `visualization`
(verify gmsh_knowledge ref; policy-stale PyVista), `gmsh_animation` (verify
.step/.msh regenerability per the mesh-preservation policy).

### Phase 4 -- HIGH-REFERENCE CORE-METHOD CORPORA (heavy migration)

Reference migration is the bulk of the work; per-file review required. Do NOT
blanket-delete.
- `equivalence_source`, `induction_heating`, `cubit_panels` (KEEP accel_magnet/
  fixtures; prune inductance/ experiment scripts), `ngbem_diagnostics`.
- `vim` and `clebsch_hodograph` -- coordinate the two notebooks (shared
  Clebsch / streamfunction line) to avoid duplication; migrate the named-file
  refs in docs + MCP knowledge first; KEEP `vim/reference_yano_msc/golden/`.
- `peec_integration` -- multi-session; migrate the 2 radia.py citations first.
- `kelvin_transformation` -- promote the classic demo families; prune
  `AdaptiveMesh/`; leave `DtN_spectrum/` and `Cubit_1_4_p_convergence/`
  (panel-sample-backing) untouched.

### Phase 5 -- KEEP-EXAMPLE (no deletion, ever, this pass)

Verified-locked or canonical example tiers. Do not promote-away or delete:
the cross-validation registry corpus (`cubit_mesh_export`,
`build123d_netgen_gmsh_flow`, `rf_waveguide`, `acoustic_bem`,
`electric_machine`, `electrostatics`, `fem_readable`, `optimization`), the
runtime-imported `universal_relaxation_network` core files, the shipped
application tiers (`stream_function`, `ih_esim_benchmark`), Validation-Class
sets (`hysteresis`, `force_validation`, `tetra_field_accuracy_evaluation`,
`eddy_current_analytical_validation`), already-promoted `axifem`, and the
small canonical demos (`open_boundary`, `cohomology`, `hantila_solver`,
`peec_bema_convergence`, `complex_coil_geometry`, `figures`, `mathematica`,
`mesh_generation`, `maglev/*` non-research_cln).

### NEVER -- BLOCKED

- `maglev/research_cln` (435 files) -- active corpus, another agent, own
  CLN->ipynb goal.
- `kelvin_transformation/DtN_spectrum` (~122 act-scripts + HANDOFF/PATHWAY
  docs) -- active Track-A/B paper corpus, referenced by calc_streamfunction.py
  and dtn_coarse_mesh MCP knowledge. Same caution class as research_cln.

## Pre-Work Fixes (do regardless, before their topic's deletion phase)

- FIX `tests/test_team28_cln_golden.py` path: ensure it resolves
  `examples/maglev/team28` (not the nonexistent `examples/levitation/team28`).
- FIX stale knowledge ref in `radia_ngsolve/knowledge/radia.py`:
  `examples/cube_uniform_field/test_objm_minimal.py` -> `experiment_objm_minimal.py`.
- VERIFY the `.jou` comment in `src/radia/panels/samples/ih_fem_kelvin_skin_fine.jou`
  citing `filament_coil/compare_fem_vs_peec_skin.py` (file may not be in git).
- VERIFY `esim_knowledge.py` `sweep_v2/` path still exists before any
  ih_esim_benchmark pruning.

---

## Execution log (2026-06-26)

DONE (local commits; codex pushes):

- `smco_magnet_array` -> `docs/smco_magnet_array/smco_magnet_array.ipynb`
  (commit e6918b2c). Fixed bitrot: ObjHexahedron/ObjWedge magnetization arg +
  core ring is a wedge; magnetization is A/m not Tesla. VERIFIED. Strengthened
  magnetic_materials permanent_magnet knowledge.
- `mesh_fusion` -> `docs/mesh_fusion/mesh_fusion.ipynb` (commit 89d7e708).
  5 phases embedded + JSON + convergence PNG, re-runs on execute. Strengthened
  fem nonconforming_mesh_coupling ngsolve_recipe topic.

RECLASSIFIED after per-topic caller verification (the read-only inventory's
prune/promote labels were unreliable -- verify EACH topic before acting):

- `clebsch_legendre` -> KEEP (3 golden tests import its modules).
- `jou_translation_bench` -> KEEP (.jou = Mesh File Preservation protected).
- `ngsolve_user_meeting` -> KEEP (legit OCC-vs-Cubit-ACIS p-convergence demo).
- `effective_surface_impedance` -> DEFER (private deps, binary .pkl, not
  runnable standalone; careful distill needed).

NEXT verified-clean promote candidates: `bem_extractor`, `nodal_force`
(nodal_force has 1 radia_mcp ref to migrate). Re-verify each remaining topic's
refs/assets individually; do not trust the bulk inventory labels.

### Batch 2 verification (workflow wlkux188r, 2026-06-26)

Per-topic read-only verification (5 agents). Inventory "promote" labels again
unreliable -- 3 of 5 are DEFER:

- `nodal_force` -> PROMOTED (commit ed9afa09); 1 MCP ref migrated.
- `analytical_formulas` -> PROMOTE (conf 0.92, runnable, no cubit, 24 files;
  refs are MCP analytical_formulas.py + server.py). NEXT.
- `hiruma_xfem_comparison` -> PROMOTE (conf 0.9, runnable, no cubit, 5-phase
  XFEM study; refs xfem_em_hiruma + server + cln_sibc_orthogonal). NEXT.
- `background_fields` -> DEFER: 2 scripts BROKEN (MatSatIsoFrm called with 3
  positional args; pybind takes ONE list -> TypeError) + cubit_to_nastran needs
  Cubit + sphere.bdf orphan. Also fix the wrong MatSatIsoFrm signature in
  radia_ngsolve/knowledge/radia.py:2408. Fix-then-promote later.
- `simple_problems` -> DEFER: ObjArcCur called with 6 args (signature changed)
  -> TypeError; compare_magpylib needs magpylib. Fix-then-promote later.
- `mixed_galerkin` -> DEFER: 8 scripts have stale hardcoded sys.path ->
  import-break; 24 files. Path-fix-then-promote later.

bem_extractor -> DEFER (verified inline): BEMExtractor lives only in
examples/induction_heating/bem_reference/cubit_bem_extractor.py; the example's
`from cubit_bem_extractor import BEMExtractor` (src/radia path) is broken.

### Batch 3 (A/B/C + continuation, 2026-06-26)

- analytical_formulas PROMOTED+DELETED (7871cb87).
- hiruma_xfem_comparison MIGRATED+DELETED (b591f06b; notebook ae9ca2a0).
- C bitrot fixes (764418de): MatSatIsoFrm single-list + rad.Solve 4-tuple index
  (background_fields x3 + radia knowledge), ObjArcCur 8-arg (simple_problems),
  stale sys.path (mixed_galerkin cube3d 02-07).
- simple_problems PROMOTED+DELETED (8fb9943d): 4 demos -> docs notebook;
  compare_magpylib unit fix M=Br/mu_0 (now PASS 0.46%); chamfered_pole_piece
  DROPPED (ObjMltExtRtg non-planar-face misuse, lesson to memory);
  CONVERSION_NOTES.md dropped.

examples/.py: 1112 -> 1083 this session.

NEXT (dedicated session -- larger/structured, NOT a flat fold):
- mixed_galerkin (24 files): NOT golden-coupled (test_maglev_mixed_galerkin_golden
  imports the radia.maglev.mixed_galerkin SOURCE package, not the example scripts).
  But it has a _references/ support package (4 modules the demos import), 19 demo
  scripts across cube3d/cylinder/sphere/square2d/time_domain/ngsolve_validation/
  lshape3d, a _broken_simple_envelope.py (drop+distill), and .npz runtime dumps.
  Plan: keep _references/ as a support package OR inline; fold the demos into
  docs/mixed_galerkin/*.ipynb section-by-section, expect per-demo bitrot (like
  simple_problems). Refs: docs/README.md:69, cln_sibc_orthogonal.py:10.
- Then: background_fields stays DEFER (sphere.bdf protected + cubit_to_nastran);
  bem_extractor DEFER (BEMExtractor in induction_heating/bem_reference).

### Batch 4 scope: the 4 big corpora (workflow wv1jrzrq9, 2026-06-26)

ALL FOUR are ACTIVE research corpora -> conservative (addition-mostly), mirror
mixed_galerkin: add a docs showcase notebook reproducing the VERIFIED subset,
KEEP the corpus + support packages + CI-locked core, prune only clearly-dead
non-protected cruft.

- **peec_integration (289)**: NO CI lock (only stale comments + a phantom file
  compare_fem_vs_peec_skin.py). Active (filament_coil open question). KEEP:
  ngsbem_peec_demo/ (4 ngbem_* support modules), verification/ (Validation-Class,
  46 files), gmsh_models/*.msh, data/*.csv (deliberately un-ignored), filament_coil/.
  SHOWCASE: docs/peec_integration/ from peec_impedance.ipynb + paper figs +
  PRIMA-Dowell + Dowell-CF + spiral + wpt. PRUNE (verify first): model/ (orphan
  KAN/torch binary cache), ngbem/ top-level (3 stale/broken), 3 orphan verify_cln
  PNGs, benchmarks/compare_peec_ngsolve_resistance.py (scratch). Migrate
  radia.py:1279-1280 only if paths change.
- **ih_esim_benchmark (178)**: CI LOCK = test_igte_dense_sweep_golden literal-asserts
  sweep_data_dense/*.json + sweep_data/I500_f10k_Zs_side_field.json (108 runs, golden
  values). Active (Phase A/B roadmap). KEEP all sweep JSON (source-of-truth).
  SHOWCASE: docs/ih_esim_benchmark/ reproducing ONLY committed-JSON-backed figures
  (digest heatmap, dense gap map, per-DOF, Bessel cross-check) -- must NOT re-solve.
  PRUNE: 4 .sol binaries + 1 superseded I300_f10k side_field JSON.
- **vim (153)**: NO CI lock (tests/feec/ NOT tracked despite README claims). VERY
  active (edits 2026-06-22/23). SHOWCASE: docs/hdiv_vim/ curved-win subset
  (hdiv_demag_bem_singlelayer / _curved / curved_nonlinear / compare_curved_vs_radia
  / demag_spectrum_jacobi / yano_hdiv_loop_bridge) + MMMM. PRUNE (very conservative):
  3 broken yano_*_study.py, hdiv_demag_solve.py (self-declared artifact).
- **universal_relaxation_network (129)**: RUNTIME-IMPORT lock = radia_mcp urn.py
  imports universal_relaxation_network.py + relaxation_basis_library.py (shipped
  urn_fit_from_csv tool) -> NEVER delete those 2 core .py. Paper IN REVISION
  (REVIEW.md). SHOWCASE: docs/universal_relaxation_network/ paper figures +
  validate_all_datasets + ablation + spice demo (torch + committed CSVs). PRUNE:
  results/ltspice_work/ scratch only.

Execution: 4 focused conservative passes, one corpus each. Verify each prune
target before deleting (advisory inventory). All additions; small reductions.

### Batch 4 EXECUTED (2026-06-27): clean-prune + showcases for the 4 corpora

User chose "clean-prune all corpora first", then showcases. All corpora KEPT
(active research); additions + verified cruft prune only.

CLEAN PRUNE (verified each before delete):
- peec: model/ (orphan KAN/torch binary cache) + 3 orphan verify_cln PNGs +
  compare_peec_ngsolve_resistance.py (self-marked scratch).
- ih: 4 sweep_data/*.sol (binary) + I300_f10k_Zs_side_field.json (superseded).
- vim: 3 yano_*_study.py (dead `from yano_pyr_faces12_star` import).
- urn: none (ltspice_work referenced by active REVIEW.md -> deferred).

SHOWCASE notebooks added (all reproduce/display VERIFIED results, corpus kept):
- docs/peec_integration/peec_showcase.ipynb (Dowell CF + ngbem EFIE L + 4 paper figs).
- docs/ih_esim_benchmark/esim_showcase.ipynb (Bessel cross-check live + committed
  digest/envelope/per-DOF/Karl figures; plot scripts need lab-private styling).
- docs/hdiv_vim/hdiv_curved_showcase.ipynb (3 curved-win demos; MMMM excluded =
  concurrent agent's area).
- docs/universal_relaxation_network/urn_showcase.ipynb (4 committed paper figures;
  torch training not re-run, paper in revision).

DEFERRED (verification surfaced risk): peec ngbem/ top-level demos (needs
run-verify), vim hdiv_demag_solve.py (basename collides with core API), urn
ltspice_work/ (active review). prima_with_dowell_correction.py has Japanese ->
spawned cleanup task.

### Batch 5 EXECUTED (2026-06-27): 10 docs/ipynb showcase promotions

User: "10 examples を docs/ipynb に昇格". Verification workflow wf_12451772-f7a
(14 candidates, 1 Explore agent each). Override: the haiku agents tagged
electrostatics + force_validation PROMOTE_DELETE, but both are Validation-Class
keep-example per CLAUDE.md/PLAN -> ALL 10 done as SHOWCASE_KEEP (additive, corpus
kept, zero deletions; safest on the shared tree). Each commit = notebook + one
radia_mcp knowledge "SHOWCASE NOTEBOOK" pointer, verified `git show --stat HEAD`
contained only my 2 files. nbconvert --execute embeds outputs (self-contained).

The 10 (commit):
- docs/electrostatics/electrostatics.ipynb (06f74d7b) -- 5 ES identities, live.
- docs/force_validation/force_validation.ipynb (549319fb) -- 6 force/torque identities, live.
- docs/open_boundary/open_boundary_demo.ipynb (386a0529) -- DtN-CLN, live, assertion-gated.
- docs/cohomology/tomega_wire.ipynb (5a4384bf) -- T-Omega gmsh-free cut, live ngsolve solve.
- docs/cube_uniform_field/hmatrix_solver_scaling.ipynb (1a33e61e) -- live JSON scaling table
  (LU 1480s vs HACApK 25s @N=16) + 4 committed scaling PNGs; fixed stale knowledge ref
  test_objm_minimal.py -> experiment_objm_minimal.py.
- docs/solver/mmm_eigenvalue_study.ipynb (b6cb4e12) -- nullspace spectrum/scan/topology live + 3 PNGs.
- docs/complex_coil_geometry/complex_coil.ipynb (f5dfb0ce) -- CoilBuilder 8-seg coil + field slice, live.
- docs/ngsolve_integration/integration_basics.ipynb (8ac4a720) -- verify_curl_A + HDiv proj + batch, live + simkin PNG.
- docs/hysteresis/hysteresis_validation.ipynb (867fea51) -- 13-figure Play/Energy/BQM gallery (DISPLAY).
- docs/eddy_current_analytical_validation/validation_suite.ipynb (3ebf5361) -- live Dodd-Deeds rod +
  sweep + documented Smythe sphere table.

Reusable builder: scratchpad nb_builder.py (transform sys.path/__file__/Japanese/__main__/argparse +
run_cell + disp_img + verify). LESSONS: (1) generic __file__ -> os.path.join(getcwd) sub MUST run
last (the .parent/.parents[N] special-cases caused a `s[2]` substring bug); (2) prepend `import sys,os`
in run_cell (transform introduces os.getcwd; IPython tolerates it before `from __future__`); (3) NEVER
`matplotlib.use("Agg")` in a notebook cell -- it suppresses inline capture; use display(fig); (4)
nbconvert ABORTS WITHOUT SAVING on error, so a clean verify() on an unexecuted nb is a false pass --
check nbconvert exit code.

DEFERRED candidates (verified, not in the 10): hantila_solver (repo_root NameError + npz dep + NaN-
informational); tetra_field_accuracy_evaluation (heavy 3-script mesh-refinement pipeline + netgen_mesh_import
repoint -- promotable later); equivalence_source / solver_benchmarks (DISPLAY-only committed-JSON, fine later).

LATENT BUGS found while testing ngsolve_integration (flagged for follow-up, NOT fixed here):
examples/ngsolve_integration/demo_basic_field.py + demo_coordinate_transform.py -> rad.Solve "Failed to
create Interaction Matrix"; demo_scalar_potential.py -> NameError TaskManager (missing import);
demo_field_types.py -> SEGFAULT (exit 139) after "Complete". Showcase used only the 3 verified-clean
demos (verify_curl_A, demo_hdiv_projection, demo_batch_evaluation).

### Batch 5 follow-up (2026-06-27): mmm_eigenvalue_study DELETED (PI decision)

The user (Sugahara) clarified the consolidation criterion: it is VALUE, not file
count -- delete research byproducts that need not be shown to users / will not be
used going forward; keep+show valuable demos (e.g. force_validation stays a full
user-facing ipynb). Applying it: my initial conservative "keep mmm_eigenvalue_study,
it's documented" stance was OVERRIDDEN by the PI -- the study only explored the
**loop-deflation** approach whose runtime API was already removed (2026-06-09), and
**MMMM is now the official MMM H-matrix route** (+ HDiv-VIM the loop-free symmetric
route), so the study has no forward value to ship.

DELETED: examples/mmm_eigenvalue_study/ (21 tracked: 16 .py + 3 PNG + README + the
docs notebook) + 2 untracked generated artifacts (spectrum_*.npz, trunc_*.out) +
docs/solver/mmm_eigenvalue_study.ipynb (the batch-5 showcase I had just made -- the
build pipeline/lesson is reusable; the artifact had no forward value per the PI).
Reference cleanup: mmm_core EIGENVALUE_NULLSPACE intro + MSC_NULLSPACE_DEFLATION.md
line 8 now note the example was removed (theory record retained, inline Script:*.py
refs are historical). POLICY reflected in CLAUDE.md: H-Matrix Route Policy point 1
(loop-deflation R&D CLOSED, example removed, MMMM official) + File Placement Policy
(docs/<topic>/ MAY hold .py helper modules shared by the ipynb and radia_mcp for
mcp-server integration -- encouraged over inline-duplicating logic).

Net batch-5 showcases: 9 (electrostatics, force_validation, open_boundary, cohomology,
cube_uniform_field, complex_coil_geometry, ngsolve_integration, hysteresis,
eddy_current_analytical_validation). mmm_eigenvalue was promoted then deleted.

### Batch 6 EXECUTED (2026-06-27): classify-then-act under the research-lifecycle policy

Classification workflow wf_69678aa1-26d (14 candidates) under the new policy
(research -> C:\temp; outcome -> docs/ipynb or src/; else memory). User: "do 1+2
plus solver_benchmarks / background_fields / bem_extractor". Result:

- **effective_surface_impedance -> PRUNED** (commit): private deps (mylibcem /
  Compumag2025) + binary `.pkl` (Binary Policy) + superseded by src ESIM
  (`src/radia/esim_cell_problem.py`); lesson to memory; doc listing updated.
- **equivalence_source -> docs SHOWCASE_KEEP**: docs/equivalence_source/demos.ipynb
  (phase1 + null-field executed live; phase2/3 Cubit-e2e + C++ bench shown from
  committed JSON). NearFieldSource API already in src -> NOT an api move. Corpus kept.
- **solver_benchmarks -> docs SHOWCASE_KEEP**: docs/solver_benchmarks/peec_solver_benchmarks.ipynb
  (dense Ruehli vs HACApK compression + MNA crossover, read live from committed JSON;
  |Z11| consistency). Corpus + findings/.md kept.
- **background_fields -> docs SHOWCASE_KEEP**: docs/background_fields/background_fields.ipynb
  (3 ObjBckg quadrupole demos executed). Bitrot was ALREADY fixed in 764418de
  (MatSatIsoFrm single list + rad.Solve [0]/[3]) -- the classifier misread; verified
  clean by running. Corpus kept (sphere.bdf + cubit_to_nastran).
- **bem_extractor -> docs LIMITATION showcase**: verify_inductance turned out to be a
  NEGATIVE result (ngsolve.bem LaplaceSL closed-surface self-inductance is
  rank-deficient on dense meshes: +15%/-9%/-66%). Promoted HONESTLY as
  docs/bem_extractor/bem_inductance_limitations.ipynb + distilled the caveat into the
  ngsbem_inductance knowledge (which had over-claimed "LaplaceSL is sufficient").
  Corpus kept; extract_inductor (broken import + Cubit) not run.

API-ization: NONE found -- the reusable methods (NearFieldSource, ESIM, Kelvin,
hdiv_vim, peec_hacapk) are ALREADY in src. KEEP (no action, verified): kelvin_transformation
(CI-lock + DtN blocked + already promoted), tetra_field_accuracy_evaluation (Validation-Class),
clebsch_hodograph (62 golden tests + docs/clebsch_hodograph done), cubit_panels (CI-lock +
protected mesh), vim (85+ golden tests + partial showcase done), visualization (canonical viz).
PRUNE candidate not taken: gmsh_animation (.geo may be mesh-gen asset -- left). DEFER:
ngbem_diagnostics (ngbem_eddy import broken -> fix first). induction_heating: classifier said
PRUNE but it has protected .jou + bem_reference test-import -> NOT cleanly prunable (mixed).

LESSON: classifier (haiku) again unreliable on 2 of 5 (background_fields "broken" was already
fixed; bem_extractor "validation" was a negative result) -- ALWAYS run/read before acting.

### Batch 7 EXECUTED (2026-06-27): URN enrich + 2 promotes + doc-format policy

User: "find more to promote / fold into existing ipynb; URN too" + a new doc-format policy.
Discovery workflow wf_6cd5026b-cb8 (15 topics, 3-way lens promote/fold/enrich).

- **URN -> ENRICHED** (commit 2aba8ab3): docs/universal_relaxation_network/urn_showcase.ipynb
  was display-only (4 paper figures); added a LIVE urn_fit_from_csv cell on committed TDK PC47
  ferrite impedance (NRMSE 5.5%, ~36s, runtime-imports the shipped URN core) -> now a live ipynb,
  paper figures still render. (The classifier said "keep display-only, paper in revision"; the PI
  explicitly wanted URN live, and the live cell is an independent tool demo, NOT the paper training.)
- **optimization -> PROMOTE_NEW** (showcase-keep): docs/optimization/optimization.ipynb -- 2 Optuna
  constrained RF-waveguide designs (slab matching + Bragg filter) executed live (analytic-guess
  enqueue + feasibility-aware selection). Corpus + drift JSON kept. Pointer in global_optimizers.
- **maglev -> PROMOTE_NEW** (showcase-keep): docs/maglev/maglev_showcase.ipynb -- consolidated
  rendered view (alpha(s) sweep, sphere force, ellipsoid tensor, rotating-magnet Rm crossover,
  TEAM 28) from committed figures + headline JSON; golden corpus kept. Pointer in maglev knowledge.
  ALSO fixed a latent CI hole: tests/test_rotating_magnet_eddy_golden.py pointed at non-existent
  examples/levitation/ (dir is examples/maglev/) -> golden now locks the real file.
- **NEW POLICY** (commit): CLAUDE.md "Documentation Format" -- dev docs (plan/design/policy/API/
  README) = Markdown; explanations of a METHOD or IMPLEMENTATION = executable .ipynb (runs + shows
  result). Discriminator + opportunistic-conversion (no mass-convert). + File Placement Policy:
  docs/<topic>/ may hold .py helper modules shared by the ipynb and radia_mcp.

KEEP (verified, no action): peec_bema_convergence (data+README, no scripts), acoustic_bem /
rf_waveguide / electric_machine / build123d_netgen_gmsh_flow (CI-locked registry), fem_readable
(Validation-Class), mathematica (CI-locked basis ref), mesh_generation (.msh asset).
NOT cleanly actionable: cubit_mesh_export (classifier said PROMOTE_NEW 0.75 but it's CI-locked
registry + already has examples/netgen/p_convergence_demo.ipynb -> showcase-only at most, deferred);
gmsh_animation (.geo are protected mesh-gen scripts -> NOT a clean prune); visualization
(knowledge-referenced viewer-selection reference -> leave); ngbem_diagnostics (DEFER: ngbem_eddy
import broken -> fix first).

### Batch 8 (2026-06-27): docs/*.md format scan + stale-ref prune (NEW doc-format policy)

User: doc-format policy = dev docs Markdown, method/impl explanations ipynb; "find all + GO";
"strengthen MCP AND discard stale descriptions". Two read-only workflows:

(A) classify-docs-md (wf_0a4743cc, 12 clusters, 88 .md): **78 KEEP_MD / 8 ADD_COMPANION / 2 CONVERT**
-- strict criteria worked; most docs are correctly dev-docs/reference/theory-survey -> stay .md.
After per-file verify, the actionable set shrank further and was spawned as PRECISE self-contained
tasks (user preference: precise-spec chips over a fatigued tail mass-edit):
  - task_7a436b56: CONVERT docs/hdiv_vim/POLYNOMIAL_CHARGE_FIELD.md -> ipynb (the only clean convert;
    runnable radia.vim kernels; migrate 2 refs: src/radia/vim/_field.py + HDIV_VIM_CLEBSCH_BRIDGE.md).
  - task_adaf2ca3: ADD_COMPANION for stream_function theory/regularization/deformation (runnable SF demos).
  - task_b5959a9c: ADD_COMPANION peec/PEEC_SURFACE_IMPEDANCE (Dowell F/G; avoid peec_showcase overlap).
  - task_453153f0: kelvin CG-smoother -- verify demo feasibility first, else keep .md.
  SKIPS (verified): analytical_formulas.md (PDF citation index + ipynb already exists -> KEEP_MD);
  api/CoilBuilder.md (already companions docs/complex_coil_geometry/complex_coil.ipynb -> KEEP_MD);
  multipole_moment_mmm/QUADRUPOLE_ANALYTIC_KERNEL.md (concurrent MMMM agent's area -> do not touch);
  stream_function.md top-level (thin pointer, no demo -> KEEP_MD).

(B) dangling-path audit (scripted, radia_mcp + docs): ~30 files with refs to nonexistent
examples/docs paths. DONE inline (commit e7c7dfdd): hantila_polarization->hantila_solver,
NGBEM doc path ->docs/solver/, removed dangling MAGNETIC_AFTEREFFECT pointer. SPAWNED
task_c2c668bf for the ~20 examples/comsol_class refs (stale + PUBLISH-BOUNDARY: COMSOL is
lab-private -> scrub path tokens, keep physics, run policy_lint). REMAINING stale refs to clean
later (verify each): examples/CLN/ (axifem/bem_cln/cln_sibc_orthogonal knowledge), examples/cubit/
(ngsbem_inductance), examples/hierarchical_cauer_sibc/ (cln/BEM_CLN.md), examples/dtn_spectrum_
coarse_mesh_demo.py (kelvin), + missing-doc pointers (FEM_SCATTERED_FIELD / HMATRIX_EVALUATION /
MSC_QUICK_START / docs/quickstart / docs/TOOLS / docs/Cauer1-2.pdf / docs/design/). FALSE
POSITIVES (left): mesh_fusion ("former ..." historical note), sparsesolv examples/hiruma/
(different repo), peec ...(planned) placeholders, MSC_NULLSPACE_DEFLATION mmm_eigenvalue (kept as
historical-removal note).

NEW policies this batch: CLAUDE.md "Documentation Format" (md vs ipynb) + memory
feedback_doc_format_md_vs_ipynb + feedback_mcp_knowledge_prune_stale (MCP knowledge is not
append-only). Net new docs ipynbs this session: 9 examples promotes (batch 5) + electrostatics..
eddy_current + cube + URN-enrich + optimization + maglev; equivalence_source/solver_benchmarks/
background_fields/bem_extractor showcases; effective_surface_impedance + mmm_eigenvalue pruned.

### Batch 9 (2026-06-27): execute the docs/*.md conversions inline (user: "do them, ipynb as much as possible")

Done INLINE on main (verified, real validations):
- CONVERT docs/hdiv_vim/POLYNOMIAL_CHARGE_FIELD.md -> docs/hdiv_vim/polynomial_charge_field.ipynb
  (commit 1f1b7c6e): full derivation prose kept + Live validation running radia.vim kernels (flat
  field vs independent Gauss 4e-15; const volume charge vs -grad(PhiTet) 1e-9; degree-0/1/2 closed
  forms nest exactly; general assembler reduces at deg0). Migrated 5 refs (_field.py x4 +
  HDIV_VIM_CLEBSCH_BRIDGE.md), removed the .md, METHOD-NOTEBOOK pointer in hdiv_vim knowledge.
- ADD_COMPANION docs/peec/dowell_surface_impedance_demo.ipynb (commit bea43722): Dowell F(xi)/G(xi)
  live + coth(foil)-vs-tanh(slab) + radia EMMaterial.dowell_Zs cross-check (3e-15 / 3e-14); .md kept.

NOT done inline (left to the user's already-running worktree chips -- isolated, won't touch main's
examples/): SF companions x3 (task_adaf2ca3), kelvin CG-smoother (task_453153f0), comsol-class scrub
(task_c2c668bf). Reason: the SF demos are sibling-coupled and WRITE PNGs into examples/ (overwrite-
committed-data hazard -- a probe clobbered demo_planar_uniform_fem_psi.png, restored); the comsol
scrub mangled files once via a blind regex (reverted) and needs targeted edits + policy_lint; kelvin
demo feasibility is uncertain. These are safer in the isolated worktrees (or a fresh inline turn).

SHARED-INDEX HAZARD (recurred): a bare `git commit` swept codex's staged src/core/rad_relaxation_
methods.cpp into my commit -> fixed (reset --soft + restore --staged + re-commit). LESSON (added to
[[codex_claude_source_edit_mutex]]): always `git add <my files>` then `git commit -- <my paths>`
(pathspec), verify `git show --stat HEAD` each time. codex committed MMMM deflation (f9cb1dd4) +
peec figures (469d2417) interleaved -- my commits stayed clean.

---

## Batch-10 (2026-06-27): worktree reconciliation -- all 3 deferred conversions LANDED on main

The 3 deferred items (Batch-9) were completed in the user-started worktree chips; this batch
RECONCILES their committed branches onto main (cherry-pick the COMMITTED branch head, NOT hand-copy
uncommitted files -- see lesson). All landed with explicit pathspec, each `git show --stat HEAD`
verified to list ONLY my files.

- KELVIN (commit 055380a8, from worktree claude/ecstatic-germain-9a32ef a96fa61d): cherry-picked
  docs/kelvin/Supplement/cg_smoother_demo.ipynb (executed, 616 lines, 11 outputs, 3 figures) + the
  CG-smoother.md / KELVIN_TRANSFORMATION.md companion pointers + radia_ngsolve/knowledge/kelvin.py
  METHOD-NOTEBOOK pointer. Reproduces the doc table (mu_r=100 sphere ~2660 elems: direct eta=0.816;
  CG(20) 1.25%; CG(50) 0.00%; corr 0.976; top-10% refine overlap 97.7%).
- SF x3 (commits ad3e3d4e + 2b842cca, from worktree claude/exciting-hellman-068466 08b2feeb):
  theory.ipynb + regularization.ipynb + deformation.ipynb (all executed, English-only, 0 error cells,
  2 figs each). theory/regularization were byte-IDENTICAL to my hand-copied versions; deformation was
  the 3rd the worktree added. Pointers in theory/regularization/deformation.md + aca_tsvd.py +
  streamfunction_knowledge.py (SHOWCASE NOTEBOOKS block).
- COMSOL scrub (commit c66970c4, from worktree claude/sleepy-antonelli-0650db 45c07682): 12 files,
  removed ~40 dangling examples/comsol_class/ path refs (lab-private, commercial-named corpus) while
  PRESERVING every validation number/physics claim. Verified: 0 comsol_class refs remain; all .py
  compile; top-level policy_lint 7/7 + radia-mcp publish-boundary lint both PASS.

DUPLICATES (NOT merged -- already on main from Batch-9 inline): worktree claude/jolly-ellis-07201c
(PEEC Dowell == main bea43722) and claude/vigilant-rubin-2c7876 (POLYNOMIAL == main 1f1b7c6e). Their
deliverables exist on main; the worktrees are redundant.

LESSON (worktree reconcile): the worktree sessions were STILL ACTIVE and committed mid-reconcile
(SF advanced from 2 uncommitted notebooks to a 3-notebook commit while I worked). Prefer cherry-pick
of the worktree's COMMITTED branch head over hand-copying its uncommitted working-tree files -- the
latter races the live session. Recorded in [[codex_claude_source_edit_mutex]].

NOT touched: worktree claude/sharp-hodgkin-b5fa23 (codex MMMM + effective_surface_impedance prune,
dirty) -- codex's area. codex landed MMMM deflation commits (f9cb1dd4, 0aba78be sparse PARDISO coarse
factor) interleaved with mine; all my commits stayed clean (pathspec).

Worktree dir teardown left to the user/UI (the 5 redundant worktrees are clean+committed; their
claude/* branches preserve every commit, so removal is recoverable -- but force-removing a possibly
UI-tracked session dir is the user's call).
