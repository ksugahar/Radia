# HDiv-MMM 加速器電磁石トポロジー最適化まとめ

更新日: 2026-08-16
対象: Radia 4.95.52 release candidate

## 停電前の安全状態

- `hibino` で実行していた 42,480 DoF の再探索はユーザー指示により停止した。
- 停止後、`ffag_two_stage_42k` を含むリモート Python プロセスが 0 件であることを確認した。
- 2026-08-13 に `mdx` で map-gate 計算を最初から再実行し、完走した。
- 停電前の停止時点では MATLAB build は未実施だった。復帰後の MATLAB/MEX
  検証結果は後段に記録する。
- 作業ツリーには本件以外の共有中変更もある。reset、checkout、掃除を行わず、そのまま保存している。

## 現在の設計問題

当面の PoC は円形加速器の閉軌道問題ではなく、入口から出口までの 1 パス問題である。

- 入力: 運動量ごとの固定 design orbit と、各軌道まわりの目標 transfer matrix
- 電磁場: CoilBuilder の固定コイル源と、空気体積メッシュを持たない HDiv-MMM
- 離散化: HEX + BDM1
- 設計変数: 鉄候補 HEX の二値 active/inactive 状態
- 出力: 一つの磁石形状で複数運動量の bend-field と one-pass transfer matrix を近似する active set
- 現段階では Trafo や Sculpt は仕上げに入れず、まず Lego 状態の粗最適化を成立させる。

## 実装済みの探索鎖

### 1. Transfer-matrix 誤差から軌道磁場目標へ

`src/radia/accelerator_magnet_topopt.py` の optics inverse は、forward-mode AD で field-to-map Jacobian を作る。保持した TSVD 部分空間の中で Chebyshev LP を解き、最大 band-normalized 誤差を最小化する。

- design finite difference は使わない。
- 磁場座標 `B` / `dB/dx` をseedし、生成行列のスカラー演算、各区間の行列指数、区間積を自動微分する。
- 行列指数は Fréchet 微分を正確なAD primitiveとして使う。有限差分はテストの独立oracleだけである。
- 近零特異モードによる巨大な磁場補正を避ける既定 TSVD cutoff は `1e-3`。
- candidate 要素は optics inverse に直接入れない。

`run_transfer_matrix_material_inverse_pipeline` で、次の5段を一つの監査可能な結果として取得できる。

1. 磁場分布
2. forward-mode ADによる transfer matrix
3. `target - realized` transfer-matrix差分
4. band正規化 TSVD + Chebyshev minimaxによる差分磁場
5. ACA -> thin QR -> TSVD + 0--1 LPによる二値材料候補

### 2. 軌道磁場目標から鉄候補へ

`src/radia/topology_optimization.py` の材料 inverse は、全候補に対する Abe--Murata / DUCAS 型の ACA--QR--TSVD である。

- exposed な追加候補と削除候補をすべて低ランク screen に入れる。
- 正の等価磁化は追加、負の等価磁化は削除の提案になる。
- 連続予測を 0--1 LP で whole-element 状態へ射影する。
- 受理判定は完全 active-system solve による。グレー材料と design finite difference は使わない。
- `exact_candidate_limit` は全候補 TSVD の候補数ではなく、棄却後に高価な block-Schur / 完全 solve へ渡す代表 front の上限である。
- bounded nonmonotone beam (`exact_beam_width`, `exact_beam_depth`) は、完全 solve 済みの浅い barrier state だけを保持する。最終設計として悪化 state は返さない。

### 3. 固定 1 パス outer loop

コミット `9b258c4d2` までに、材料更新後に optics inverse を再線形化する固定軌道反復を実装した。

今回さらに、局所 field-correction target の改善だけで材料更新を採用しないよう、元の bend-field / transfer-matrix 目的に対する exact map gate を実装した。

- 新規 `FFAGFixedOrbitMapTrial` が各厳密候補の移動率、前後の最大比、受理可否を保持する。
- `map_trust_region_trials` 回、whole-element material move fraction を半減して再提案する。
- 元の one-pass 最大比を減らさない候補は棄却する。
- 全候補が棄却された場合、最後の悪化形状ではなく incumbent を返す。
- validation JSON に `map_trust_history` を保存する。
- field-target lane が停滞した場合、forward-mode ADの field-to-map Jacobian を全候補材料縮約へ直接合成する `direct_map_oracle_fallback` を使用できる。
- direct oracle 専用の bounded nonmonotone beam と、全候補 ACA--QR--TSVD で seed した複数 connected graph-front を明示的に指定できる。
- beam の各完全 solve 済み状態について、深さ、親/incumbent/候補比、累積追加・削除要素、solve反復数、探索pathを `exact_search_trace` に保存する。
- validation restart は最終 binary active-set を明示要素IDとして保存する。旧JSONは親restart chainとaccepted historyを再生して互換復元し、restart時は保存済みsource scaleを固定する。

### 4. 三次Taylor map、xyクロスターム、到達可能性

`src/radia/accelerator_taylor_topopt.py` を追加し、`u_out = R*u + T[u,u]/2 + U[u,u,u]/6 + O(u^4)` を二値HDiv-MMM topology目的へ接続した。

- 二次raw場応答はnormal dipole、normal/skew quadrupole、normal/skew sextupoleの5block。三次ではnormal/skew octupoleを加えた7block。
- native GridFunctionと同じ9点調和投影をHDiv線形functionalにし、材料候補からmultipoleまでは有限差分を使わない。
- `R/T/U` の値は既存native C++ variational-map核をsource of truthとする。
- 同じRK4とTaylor-map合成式をforward-mode ADで微分し、octupole直接項だけでなくchromatic項とsextupole等のlower-order cascadeを含む `dR` / `dT` / `dU` を作る。
- skew quadrupoleで一次xy結合、normal/skew sextupoleで二次項、normal/skew octupoleで三次xy項を制御対象にできる。
- `certify_taylor_map_reachability` はband正規化した目標差を局所AD像へTSVD投影し、dipole / `R` / `T` / `U` 別の不可到達残差を返す。これは現在点の局所certificateであり、大変位のglobal reachability証明ではない。
- 二次・三次のmaterial inverse pipelineは、multipole -> AD `R/T/U` -> 目標差 -> TSVD certificate/correction -> ACA/thin-QR/TSVD材料候補 -> native exact gateを一つの結果に保持する。
- 二次・三次のwhole-element optimizerは、AD JacobianをHDiv候補応答と縮約し、採用する二値形状を完全active-system solveとnative map再評価で受理する。
- MATLABには `radia.python.acceleratorTaylorTopopt` の明示的batch fallbackを追加した。

高速テストでは、2 HEXのうち1要素を追加して一次xy結合と二次`T_xy`を合わせる経路、および別の2 HEX問題でskew octupoleの三次`U_xxy`を合わせる経路が通った。到達不能なsigma入力依存`U`目標は100 band分の不可到達残差として分離された。ただし、42k FFAG実モデルは高次目的で未実行である。このraw Taylor経路はpiecewise-constant paraxial body-fieldモデルであり、正準Lie化は次節の独立した厳格経路で実装した。

### 5. 正準 Lie map、Dragt--Finn f3/f4、自動微分

`src/radia/accelerator_lie_topopt.py` を追加し、`(x,px/p0,y,py/p0,ell,delta)` の正準座標で、曲線座標body-field Hamiltonianを4次まで展開した。

- exact parentは `-(1+h*x)*sqrt((1+delta)^2-px^2-py^2) - a_s + H_ref(delta)`。有限速度slip、sector focusing、normal/skew quadrupole・sextupole・octupoleを含む。
- C++の `BuildCanonicalBodyHamiltonianJet` を値のsource of truthとし、`H2/H3/H4` と `A=JH2`、`F2=JH3`、`F3=JH4` をpybindとstandalone MEXの両方へ公開した。
- Pythonは7 multipole変数すべてに対する解析forward-mode tangentを保持し、毎回native C++値と照合する。
- `M = R o exp(:f3:) o exp(:f4:) + O(z^4)` のDragt--Finn分解を実装した。`f3`自己cascadeを除いてから`f4`を抽出し、生成関数対称性、再構成誤差、formal symplectic residualをhard gateする。
- `apply_dragt_finn_map` はcubic/quartic Hamiltonian flowをimplicit midpointで解き、有限振幅でもsymplecticな写像として評価する。
- raw `R/T/U`だけでなく `f3/f4` とその自動微分を返す。目標 `R/T/U` 自体がformal symplecticでなければ最適化開始前にrejectする。
- 既存のTSVD到達可能性分解、ACA--thin-QR--TSVD材料inverse、whole-HEX二値提案、完全active-system solve、exact map gateをそのままLie目的へ接続した。
- Mathematica/Wolfram Languageのtracked scriptはexact Hamiltonian展開、`f3`自己cascade、`f4` kickを独立導出してJSON goldenを生成する。runtime依存ではない。
- MATLABにはnative `radia.beam.canonicalBodyHamiltonianJet` と、`radia.python.acceleratorLieTopopt` の明示的batch fallbackを追加し、parity manifestへ登録した。

適用範囲はsource-freeなpiecewise-constant body multipole through octupole、map order 3（generator degree `f4`）である。longitudinal fringe/edge vector potential、closed-orbit recovery、任意次数normal formはまだ別課題であり、暗黙近似しない。詳細契約は `validation_test/ffag_topopt/LIE_MAP.md` に記録した。

検証結果:

- Wolfram Language symbolic oracleを再実行し、tracked `lie_map_symbolic_reference.json` を生成した。
- C++ `test_rad_beam_transfer.exe`: all tests passed。`A^T J + J A = 0` と `-J F2 = H3`、`-J F3 = H4` を確認した。
- Lie focused Python: 9/9 passed。Mathematica係数、Dragt--Finn自己cascade、非Hamiltonian mapの検出・修復、有限振幅symplecticity、自動微分、4次局所誤差、TSVD材料inverse、2 HEX完全solveを含む。
- 加速器/Lie/Taylor/FFAG/isochronous/topology focused Python: 195/195 passed。
- MATLAB native beam MEX + explicit Python fallback: 17/17 passed。新規2 wrapperのCode Analyzerは0 issue。
- repository-wide Python: 2,280 passed、16 skipped、1 failed。残る1件は本件外の既存source-text guardで、対象関数の切り出し終端が後続 `ParallelFor` まで含むため失敗している。共有差分なので本作業では変更していない。

主な実装ファイル:

- `src/radia/accelerator_taylor_topopt.py`
- `src/radia/accelerator_lie_topopt.py`
- `src/radia/ffag_topopt.py`
- `src/radia/topology_optimization.py`
- `tests/test_accelerator_taylor_topopt.py`
- `tests/test_accelerator_cubic_taylor_topopt.py`
- `tests/test_accelerator_lie_topopt.py`
- `tests/test_ffag_topopt.py`
- `tests/test_topology_optimization.py`
- `matlab/+radia/+python/acceleratorTaylorTopopt.m`
- `matlab/+radia/+python/acceleratorLieTopopt.m`
- `matlab/python_api_parity_manifest.json`
- `validation_test/ffag_topopt/validation_ffag_full_field_c_yoke.py`
- `validation_test/ffag_topopt/README.md`

### 6. HCurl A-map track と独立 B-map RK の先行照合

Lie map の検証を進める前に、同一の design orbit、Bishop/RMF frame、`h(s)`、
初期正準状態を使って、次の2経路だけを直接比較した。

1. HCurl `A` GridFunction を多項式化せずに使う正準 s-RK
2. HDiv-MMM の独立 B coefficient を使う Cartesian Lorentz RK

この照合により、当初の水平運動量差の主因を順に分離した。

- `PlanarDesignOrbit` の横軸は `e_x=bend_axis x tangent` なので、正の旋回曲率
  `h` に対する longitudinal metric は `1-h*x` である。A-RK が使っていた
  `1+h*x` を修正し、A/B とも物理的に同じ正の Lorentz 符号へ統一した。
- 16軌道区間に対して loft が32縦セルだった条件では、区間中央が HCurl の
  縦セル面に一致した。`A_s` はその面の法線成分なので直接値が一意ではない。
  軌道点を33点にし、1軌道区間と1縦セルを一致させた。
- HCurl射影前の raw `A` で `curl(A)` と独立 `B` を診断した結果、残差の主因は
  HDiv磁化体積積分の求積不足だった。これは診断だけに curl を使ったもので、
  A-RK本体は引き続き `A` を直接使う。
- `VectorPotentialCoefficientFromSolution` に beam loft 全頂点を対象とする
  target-adaptive source quadrature を追加した。近接source要素だけ高次数にし、
  遠方要素は低次数に保つ。選択次数ヒストグラムとsource点数を結果へ保存する。
- 単一状態だけでなく、ゼロ偏差と `x,px,y,py,delta` の基底摂動を一括比較する
  batch A/B APIを追加した。さらに、±基底を推奨入力として、ゼロ軌道の共通
  バイアスと一次 transfer-matrix のA/B差を affine fit で分離して返す。

C型電磁石の粗い研究用問題では、raw `curl(A)-B` の最大差は一様求積で
q5: `0.127688 T`、q8: `0.038291 T`、q10: `0.026229 T`、
q12: `0.015876 T`、q14: `0.010107 T` と低下した。適応q6--q14は
全域q14の場と出口状態を再現しながら、source点数を279,552から94,988へ
削減した。適応最大次数を16、18、20へ上げたときのraw最大差はそれぞれ
`0.006237 T`、`0.003881 T`、`0.002363 T` だった。

同一の非零初期状態に対する最大 A/B 出口差は、適応最大q14、q16、q18、q20で
それぞれ `3.1168e-5`、`1.8934e-5`、`1.0949e-5`、`6.3974e-6` まで低下した。
適応q6--q20の6ケース基底比較では、全ケースの最大差は `7.7703e-6` だった。
ゼロ偏差の共通 `px` 差は `-7.6680e-6`。ゼロ偏差差を除いた one-sided
route-Jacobian差の例は、`R_xx=3.3548e-4`、`R_pxpx=1.9205e-3`、
`R_py,y=-1.8205e-2` である。最後の値は独立B経路の同成分約35.1に対して
約0.052 %だった。

この結果は A/B 経路が一致方向へ収束することを示すが、最終認定ではない。
次は磁性体mesh収束、横差分幅収束、±基底の中心差分、aperture内の複数振幅を
確認する。その後にだけ Lie truncation error の比較へ戻る。

## 42,480 DoF 実計算

共通条件:

- host: 初期比較は `Hibino`、停電復帰後の map-gated / restart計算は `mdx`
- mesh: 1,180 HEX、42,480 BDM1 DoF、空気体積要素 0
- 初期固定 return-yoke: 720 要素
- energy: 31 MeV、250 MeV
- fixed one-pass orbit: 各 16 segment
- `mu_r=1000`
- H-matrix: `eps=1e-4`, `eta=2`, leaf 128
- linear solve: tolerance `1e-4`, max 300、deflation 8、recycle 4
- 32 threads、CoilBuilder arc 24 segment / loop

### 最小 exact front の基準計算

結果 artifact: `ffag_fixed_one_pass_two_stage_42k_final.json`

- active: 720 -> 728
- 受理した追加 batch: `[96,100,108,348]`、`[97,101,109,581]`
- 最大 band ratio: 40.8411024 -> 40.7362803
- 最終 orbit-field ratios: `[40.7363, 34.6138]`
- 最終 transfer-matrix ratios: `[18.1077, 7.53234]`
- H-matrix build: 62.565 s
- fixed-orbit optimization: 165.976 s
- total: 228.818 s
- peak working set: 約 1.293 GB
- H compression: 0.04663、max rank 59
- target band には未収束。`status=pass` は実行・topology gate 合格であり、設計目標達成を意味しない。

### exact front 4 / trust 3 / beam 2x2 の比較

結果 artifact: `ffag_fixed_one_pass_two_stage_42k_exact4_beam2.json`

- active: 720 -> 734
- 4 batch を受理し、従来停止した第3反復より先へ進んだ。
- 各反復で 3 個の候補束を完全 solve した。
- nonmonotone accepted depth はすべて 0。今回の前進は barrier crossing ではなく、複数 trust 候補の比較による。
- しかし元の最大 band ratio は 40.8411040 -> 46.7172913 と悪化した。
- total: 418.864 s、うち H-matrix build 63.454 s、optimization 355.119 s。
- この形状は採用不可。この結果から、局所 field-target 目的と元の transfer-matrix 目的の間に exact outer acceptance gate が必要だと判明した。

### exact one-pass map gate を入れた再計算

結果 artifact: `ffag_fixed_one_pass_two_stage_42k_map_gate.json`

- host: `mdx`
- active: 720 -> 728
- 最大 band ratio: 40.8411038 -> 38.1627254（6.558 %低下）
- 受理 trial: 3、棄却 trial: 5
- 受理 move fraction: 0.01、0.005、0.01
- 最終 orbit-field ratios: `[37.1553, 38.1627]`
- 最終 transfer-matrix ratios: `[18.0289, 7.0290]`
- 最後の optics 反復では 0.01、0.005、0.0025、0.00125 をすべて棄却し、直前の728要素 incumbentを保持した。
- 受理された全 trial で元の one-pass 最大比が厳密に低下し、active countと最終比が最後の受理 incumbentに一致することを機械確認した。
- H-matrix build: 67.528 s
- fixed-orbit optimization: 736.422 s
- total: 804.381 s
- peak working set: 1.207 GiB
- H compression: 0.04663、max rank 59
- `converged=false`。map gateの実モデル成立は確認できたが、目標bandには未到達。

### direct map-Jacobian と非単調 graph-front の再開計算

再開比較は保存済み source scale `6.037204495327173` を固定して実施した。最初の
restart初期比は保存値との差が `5.56e-6` relativeであり、同一incumbent比較になって
いることを機械確認した。

1. 単一 direct map-Jacobian proposal:
   - 結果 artifact: `ffag_fixed_one_pass_two_stage_42k_map_oracle_scale_locked.json`
   - active: 728 -> 728、要素442を削除し507を追加
   - 最大比: 38.1629377 -> 37.9477098（0.564 %低下）
   - field-target案はactive-setを変えられず、direct oracle案だけがexact map gateを通過した。
   - total 204.791 s、H-matrix 66.359 s、optimization 138.140 s。
2. 同じ単一proposalをもう1反復すると、field-target / direct oracleともactive-setを
   変えられず、37.94771で停止した。この結果から単純な局所proposalでは不十分と判明した。
3. direct oracleに graph-front 4案、beam幅2、深さ2を与えた第1反復:
   - 結果 artifact: `ffag_fixed_one_pass_two_stage_42k_map_oracle_graph4_beam2x2.json`
   - 深さ1で 37.9477065 -> 39.2818683 と一度悪化した完全solve済み状態を経由した。
   - 深さ2で 37.6991927 へ抜け、exact map gateが受理した（0.655 %低下）。
   - incumbentからの累積差は追加 `[90,463,513,516,1140]`、削除 `[507]`、active 728 -> 732。
   - total 400.382 s、H-matrix 65.361 s、optimization 334.734 s。
4. 新incumbentから同じ探索を継続した第2反復:
   - 結果 artifact: `ffag_fixed_one_pass_two_stage_42k_map_oracle_graph4_beam2x2_iter2.json`
   - 最大比: 37.6987363 -> 37.5821923（0.309 %低下）。
   - 累積追加 `[446,450,492,1136]`、削除なし、active 732 -> 736。
   - 最終 orbit-field ratios: `[37.5626, 37.5822]`
   - 最終 transfer-matrix ratios: `[19.7555, 7.08660]`
   - total 542.014 s、H-matrix 66.030 s、optimization 475.687 s、peak 1.205 GiB。

元の720要素形状からの最大比は 40.8411038 -> 37.5821923、累積7.979 %低下した。
すべて binary topology、空気体積要素0、完全active-system solve、original map gateで
受理している。ただし最大比はまだ1を大きく上回り、目標transfer matrixは未達である。

## 性能上の結論

- ACA--QR--TSVD 選択は通常 0.04--0.20 s 程度で、支配的ではない。比較計算の一例だけ 1.36 s。
- 完全 HDiv-MMM active-set solve は今回約 16--17.5 s / 回。
- ChargeGram 構築は約 65--67 s。
- 探索幅を広げたときの主コストは完全 solve の本数であり、Schur/ACA--QR--TSVD 自体ではない。
- map gate の探索と内側 trust 探索を同時に大きくすると同じ縮小候補を重複評価しやすい。再開計算では内側 `proposal_trust_region_trials=1`、外側 `map_trust_region_trials=4` としている。

## 検証状態

- map gate 実装前の focused suite: 100 tests passed。
- direct oracle、restart、beam trace、厳密深さ上限、累積履歴まで含む focused suite:
  `tests/test_topology_optimization.py::test_hdiv_mmm_exact_beam_crosses_one_worsening_lego_state`、
  `tests/test_ffag_topopt.py`、`tests/test_accelerator_magnet_topopt.py` の17 tests passed。
- forward-mode AD、明示的5段pipeline、二次`R/T` topology追加後、`test_accelerator_taylor_topopt.py`、`test_accelerator_magnet_topopt.py`、`test_ffag_topopt.py`、`test_isochronous_topopt.py`、`test_topology_optimization.py` の164 tests passed。
- AD Jacobianは独立の中心有限差分oracleと一致し、局所pipelineテスで材料候補10が選択され、予測最大band比が1未満になることを確認した。
- 二次ADは全10 multipole方向で中心有限差分と一致し、Python ADの `R/T` 値とnative C++値の一致gateも合格した。
- 2 HEXの完全HDiv-MMM solveで、skew quadrupoleとskew sextupoleを持つ要素を選択し、一次xy結合と二次`T_xy`を同時にband内へ入れた。
- 三次ADはdipoleからskew octupoleまで全7方向で独立の中心有限差分と一致した。確認時の最大スケール誤差は `1.4e-10`、Python ADの `R/T/U` 値とnative C++値の一致gateも合格した。
- octupoleを含まないsextupole-only入力から非零のdelta-free `U` が生じるcascade、drift chromatic `U`、skew-octupole xy `U` を個別に確認した。
- 三次focused testsは10/10 passed（二次4 + 三次6）。三次2 HEXの完全HDiv-MMM solveではskew-octupole要素を選び、選択した`U_xxy`をband内へ入れた。
- 新規三次6件を含む関連suiteは171/171 passed。MATLAB explicit Python fallbackの三次`U_xxy`回帰を追加し、`test_python_fallback.m`は10/10 passed。
- map gate のテストでは、巨大に悪化する第1候補を棄却し、move fraction を 1.0 -> 0.5 に縮小した第2候補を受理し、その後の再線形化まで確認した。
- `py_compile` と対象差分の `git diff --check` は合格。改行コードに関する既存の LF/CRLF warning のみ。
- map-gated 42k 実計算は `mdx` で完走し、悪化proposalの棄却とincumbent保持を実モデルで確認した。

## 再開手順

再開用 scratch driver と生の計算 JSON は compute host の machine-local
validation artifact とし、公開リポジトリには内部絶対パスを記録しない。

driver の主要探索設定:

```text
material/optics iterations = 1 per checked restart
material move fraction = 0.01
proposal adjoint count = 4
exact candidate limit = 4
inner proposal trust trials = 1
map trust trials = 1
primary field-target beam width/depth = 0/0
direct map oracle fallback = on
direct map graph-front proposal limit = 4
direct map beam width/depth = 2/2
beam barrier fraction = 0.25
```

推奨再開順序:

1. `git status --short` で共有作業ツリーを確認し、他者変更を消さない。
2. `python -m pytest tests\test_ffag_topopt.py tests\test_accelerator_magnet_topopt.py -q` を再実行する。
3. `mdx` / `hibino` の空き状況を確認する。今回の復帰後計算はidleな `mdx` を使用した。
4. 最新の `src/radia/ffag_topopt.py`、validation runner、scratch driver を計算ホストへコピーする。
5. 出力 prefix が未使用であることを確認する。
6. detached job にせず、次を前景で実行する。

```powershell
ssh mdx "python -u C:/temp/ffag_two_stage_42k/profile_ffag_42k_two_stage.py"
```

7. 完了後、JSON の `optimization.map_trust_history` が全 accepted step で単調減少していることを確認する。
8. `converged=false` の場合は `status=pass` と設計達成を混同しない。

## 次に解くべき課題

1. 37.5822の明示active-setからchecked restartを続け、graph-front/beamの改善率が飽和するまで1反復ずつoriginal map gateで確認する。
2. `exact_search_trace` をmap成分別に集計し、どのbarrier stateがbend-field / transfer-mapのどの誤差モードを改善したかを保存する。幅をむやみに増やす前に候補多様性を評価する。
3. ChargeGramは各restartで同一なのに毎回約66 s再構築している。長い探索では同一前景プロセス内で複数checked iterationを実行し、H-matrixを再利用する。
4. 高価な validation は段階チェックポイントを保存し、数値計算後の report serialization エラーで全結果を失わないようにする。
5. 現在の source scale は約 6.04、実効電流は約 1.51 MA / coil で大きい。最終 PoC では CoilBuilder の幾何・巻数・電流密度を現実的な設計変数へ改良する。
6. transfer bands 達成後にだけ Sculpt / TET 切り直し / Trafo 仕上げへ進む。現状は Lego 粗最適化の探索法を確立する段階である。

## 正直な到達境界

- HDiv-MMM、固定 1 パス optics inverse、全候補 DUCAS ACA--QR--TSVD、二値 LP、完全再解析、topology gate、選択的な三次`R/T/U`・xy結合目的、局所TSVD到達性certificateまでは接続済み。
- 42k DoF の空気メッシュ不要 HEX/BDM1 モデルで、完全 solve を伴う材料更新が実際に走ることは確認済み。
- 任意の数値`R/T/U`は原理的に実現できない。実現対象は現在のMaxwell/固定軌道/multipole/二値材料モデルの下で到達可能なmapである。TSVD残差は局所到達性certificateであり、global nonlinear reachabilityを保証しない。新しい正準Lie経路はbody-fieldモデル内のformal Hamiltonian symplecticityを保証するが、fringe/edgeやclosed orbitを含む実機全体の保証ではない。`eta=0` / `eta'=0`、実FFAGでの高次band達成、最終 FFAG 磁石形状はまだ達成していない。
- field-targetだけでは局所目的と元のmap目的がずれ、単一direct proposalも37.95で停滞した。forward-mode AD map Jacobian、ACA--QR--TSVD-seeded connected graph-front、bounded nonmonotone beamを統合すると、実モデルで一度悪化するbarrierを越えて2反復連続で改善し、初期比から7.979 %低下した。
- これは探索法が前進した証拠だが、最終最大比37.58は依然として許容bandの37.58倍である。任意transfer matrix再現、`eta=0` / `eta'=0`、最終FFAG磁石形状を達成したとは扱わない。

## EarlyTimes / native beam C++化

旧EarlyTimesのファイル形式やグローバル状態との互換ではなく、現在のRadia/NGSolve
設計に合わせたclean-room実装を進めた。二つの経路を明示的に分けている。

1. NGSolveの場を点評価する直接粒子追跡
2. 基準軌道の移動座標系で場を多極展開し、`A/F2/F3`と`R/T/U`を作る解析経路

### コミット済みのnative追跡基盤

コミット`040f17e9d`で以下を確定した。

- SI単位の`ParticleSpecies`、`ReferenceParticle`、`CartesianState`
- 零場、一様電磁場、相対論的Lorentz方程式
- 独立変数`time`、`path_length`、`azimuth`
- Classical RK4と相対論的Boris2
- 全状態、全step、場sample、不変量を保持する固定刻み`Tracker`
- C++、pybind11、standalone MATLAB MEXが同じC++核を使用
- 電場がある場合、運動量保存誤差を適用外`NaN`とする診断

確認済み結果:

- C++ beam transfer/dynamics CTest: 2/2 passed
- native beam dynamics Python: 8 passed（新Pydを隔離ロード）
- MATLAB/MEX beam dynamics: 7/7 passed
- MATLAB Code Analyzer: issue 0
- `Build.ps1 -MatlabMexOnly`: succeeded

### 未コミットの高次multipole経路

共有作業ツリーには次の実装が残っている。

- 各基準stationで中心1点と円周8点をlive NGSolve `GridFunction`から評価
- 右手系移動frame上で
  `By + i Bx = sum Cn (x + i y)^n`を3次まで復元
- normal/skewのdipole、quadrupole、sextupole、octupole係数とfit残差を保持
- 多極係数からparaxialな`DynamicsJet6`を生成
- `(x, px/p0, y, py/p0, sigma, delta)`で`1/(1+delta)`の色収差項を展開
- 既存変分核へ`A/F2/F3`を渡し、region attribution付き`R/T/U`を生成
- `NGSolveGridFunctionField`による直接点評価追跡

並行作業でC++ beam-transfer testと、隔離した新Pydによる
`test_beam_gridfunction_native.py` 5件の通過が記録された。ただし、その後にMEX、
FFAG map gate、公開APIの差分が追加されているため、最終release判定では全て再実行する。

物理的には、これは電流のない横断面の調和多極展開と明示したparaxial方程式である。
完全な曲線座標Hamiltonianの全3次項、一般3D縦方向微分、fringe/edgeを含む完全な
symplectic map、closed-orbit探索、Lie/適応積分まで完成したとは主張しない。
この時点の停止境界は、後述の2026-08-14正準Lie実装によりbody-field `f3/f4` まで更新された。

## リリース統合作業の停止点

handover文書をコミットする直前の実装基準HEADは
`28eb2b33f869c2e01adb68656b64f854fdf4c9c5`、`origin/main`は
`ce7f7c9e91f42459ee1a3cd98321277c08c8bb5c`だった。ローカルは17コミット先行、
リモート側の未取得コミットは0だった。

今回確定した主要コミット:

| SHA | 内容 |
|---|---|
| `0ab6e53d5` | native beam transfer、非線形reactor、統合LTspice、Simulink配布基盤 |
| `c6ae62e1e` | Gmsh電磁後処理verbと公開contract test |
| `c260f3cfd` | Cubit RVE label、材料別closure、誤誘導しないclosure診断 |
| `040f17e9d` | native Lorentz/RK4/Boris、pybind11、MEX、MATLAB API |
| `9b258c4d2` | FFAG material更新後のoptics再線形化 |
| `28eb2b33f` | Radia `4.95.51` / radia-mcp `1.4.36` release note |

この統合作業で確認した追加test:

- Gmsh MCP/表示contract/後処理: 135 passed
- Cubit closure契約、FFAG、accelerator topology: 37 passed
- FFAG/accelerator focused再実行: 13 passed
- Simulink release package: 11 passed

### 未実施のrelease操作

停電準備を優先し、以下は実行していない。

1. 未コミット高次beam / FFAG map-gate差分の完成とcommit
2. 最新`origin/main`への最終rebase
3. `claude/cyclic-image`の2コミットの`--no-ff` merge
4. 最終SHAでのpre-push matrixとGitHub CI
5. `main` push
6. annotated tag `v4.95.51` / `radia-mcp-v1.4.36`の作成と個別push
7. 最終SHAからのMEX + SLX package生成
8. 同一SHA・同一ZIPでの`release_qud all`と`release_qud done`

rebaseは開始前に共有作業から未コミット差分が入ったためGitが停止した。stash、
autostash、checkout、reset、revertは行っていない。

`claude/cyclic-image`には`f4adb9b77`（cyclic N-fold rotational images）と
`5d6d7096f`（Sculpt hex cyclic validation）が残っている。レビューは済んでいるが、
現在のWIPをcommitしてからmergeし、競合時はC++/pybind双方の登録を保持する。

## 再開時のレビュー指摘

未コミット差分は少なくとも次を直してからcommitする。

1. GridFunction結果schemaが`maximum_map_order`だけを見ている。field multipole
   orderが2/3なら、map orderが1でもmultipole schemaにする。
2. `fit_model`がorder 2でも「through order 3」と表示する。実際の1/2/3を出す。
3. `NGSolveGridFunctionField::Evaluate`で位置3成分のfinite検査を行う。
4. MATLABの`propagateGridFunctionMultipoleMap.m`と`trackGridFunction.m`、parity
   manifest、README、MEX testを完成させる。低水準MEX commandだけで公開完了としない。
5. FFAG map gateで全proposal reject時にincumbentを返す契約をtestで固定する。
6. `map_ratio_tolerance`不正時のerrorをiteration count用errorから分離し、validation
   CLIのインデントを整える。
7. 高次mapの物理的適用範囲をPython/MATLABの両docで明示する。

## 作業ツリー保全と再開順序

未コミットのbeam transfer/NGSolve/pybind/MEX/Python、FFAG map gate、対応testと
validation文書は削除・resetしない。`sculpt_input.tex`と
`sculpt_process_control.tex`も今回未操作の生成物である。

停止時にはSculpt調査のPID `16516`（`limiting_knob.py`）とPID `23588`
（`lane_repro.py`）が動作していた。停電後は途中成果を確認してから再計算する。

再開順序:

1. `git status --short`で共有差分を確認
2. 上記レビュー指摘を修正
3. C++ / Python / MATLAB focused testとCode Analyzer
4. 未コミット差分を論理単位でcommit
5. `origin/main`へrebase
6. cyclic-imageを`--no-ff` mergeしてfocused test
7. clean release worktreeでfull build、MEX、SLX package、CI
8. `main`とannotated tagをpush
9. 同一SHA/ZIPで`release_qud all`、続いて`release_qud done`

## 2026-08-13 停電復帰後の EarlyTimes 作業

このタスクでは EarlyTimes の clean-room beam 経路だけを再開した。併存する FFAG
map-gate と Sculpt の共有差分には触れていない。上記「再開時のレビュー指摘」の
うち beam 関係の 1--4 と 7 は対応済みである。

### 完成した公開経路

- GridFunction 結果 schema を field multipole order と map order の両方から決める
  よう修正した。field order 2/3 なら map order 1 でも multipole schema になる。
- `fit_model` は実際の order 1/2/3 を表示する。
- `NGSolveGridFunctionField::Evaluate` は位置3成分の有限値を検査する。
- Python の `propagate_grid_function_multipole_map` に対応する MATLAB 公開入口
  `radia.beam.propagateGridFunctionMultipoleMap` を追加した。
- live NGSolve GridFunction を各積分段で直接評価する MATLAB 公開入口
  `radia.beam.trackGridFunction` を追加した。
- MATLAB/Python parity manifest と MATLAB README を更新した。
- Python/MATLAB の説明に、このparaxial map入口が source-free transverse
  body-fieldの局所展開であり、縦方向fringe/edge微分とclosed-orbit探索を含まない
  ことを明記した。正準Lie目的は別の明示APIとして後から追加した。
- 既存の linear API は互換入口として残し、高次入口へ案内するようにした。

### 復帰後の検証実績

- `Build.ps1 -RadiaOnly -Test -Verbose`: 成功
- C++ beam transfer / dynamics CTest: 2/2 passed
- 最新 source-tree Pyd で beam GridFunction / transfer / dynamics Python: 19 passed
- `Build.ps1 -MatlabMexOnly -Verbose`: 成功
- MATLAB beam transfer / dynamics / GridFunction focused suite: 17 passed,
  0 failed, 0 incomplete
- 新規 MATLAB 公開ラッパー2本の Code Analyzer: 0 issues
- MATLAB/Python parity manifest test: 1 passed
- `py_compile`、parity audit、全作業ツリーの `git diff --check`: 合格
- `import radia`: `4.95.51` を source tree から正常ロード

公式 MATLAB MCP は既存 session への attach に2回失敗したため、同じ LAB の
MATLAB R2026a を `matlab -batch` で起動して Code Analyzer と unit test を実行した。
Simulink Agentic Toolkit 自体の installation check は PASS だった。

`build` スキルに残る GUI 資産チェックは、既に現行 tree から退役した
`ih_fem_sample.jou` と `ih_fem_kelvin_sample.jou` の旧直下名を要求するため不合格に
なる。現在の sample tree には `ih_fem_kelvin_skin_fine.jou`、
`ih_peec_bem_coarse.jou`、`ih_peec_inductance.jou` がある。本 beam 作業では旧 sample
を復元せず、GUI/Simulink 資産には変更を加えていない。

### 現在の停止境界

- EarlyTimes の場展開、`A/F2/F3`、region attribution 付き `R/T/U`、および直接
  GridFunction tracking は C++/Python/MATLAB の公開入口まで接続・検証済み。
- この節は停電復帰直後の停止境界である。以後の統合結果は release gate の
  同一コミット記録を正とする。
- FFAG 側レビュー指摘 5--6 と、map-gated 42k 実計算はこの EarlyTimes 再開では
  未実施のまま保全している。

## 2026-08-14 EarlyTimes--FFAG HDiv-MMM 融合

### Python 融合 API

`src/radia/ffag_taylor_topopt.py` を追加し、複数運動量の固定設計軌道に対して、
HDiv-MMM の multipole 観測を native EarlyTimes の二次 Taylor map `R/T` へ直接写す
経路を実装した。

- 各軌道の normal dipole、normal/skew quadrupole、normal/skew sextupole を一つの
  block objective に連結する。
- CoilBuilder incident field と HDiv 応答に同じ観測点・同じ重みを使用する。
- forward-mode RK4 Taylor AD の Jacobian を HDiv 候補縮約へ直接合成する。
- ACA--QR--TSVD、connected graph-front、非単調 exact beam、完全active-set solveの
  acceptance gateを既存のまま使用する。
- 既存 FFAG target family の指定 `R` は変更せず、理想 multipole profile から `T`
  targetを持ち上げる。今回の一次比較では `T` bandを意図的に緩くした。
- 最適化中の有限差分は使わない。有限差分は解析 Jacobian の回帰照合だけに使用した。

### 42,480 DoF の最初の A/B

`mdx` で既存の 736 HEX incumbent を再使用した。1180 HEX、42,480 BDM1 DoF、
空気体積要素0、運動量2点、各16区間、raw multipole 160行、design response 60行で
ある。

- ChargeGram: 67.222 s、compression 0.0466320、max rank 59
- multipole観測行構築: 27.943 s。一度だけ構築してbaselineと探索で再利用した。
- baseline完全solve: 114反復、18.018 s、peak working set 1.207 GiB
- 従来の平均dipole一致による電流再調整では source scale が
  6.03720 -> 3.22993となり、最大band比は40.9196となった。旧一次map incumbentの
  37.5822より悪く、平均一致はminimax目的に不適切と判明した。
- 同じ平均一致のままgraph-front 4、exact beam 2x2を1反復すると、完全solve 6本で
  736 -> 738 HEX、40.9196 -> 40.6829（0.578 %低下）。追加
  `[1113,1131,1132]`、削除`[793]`であり、二値・完全solve gateは成立した。
- 同一process合計247.548 s。うち最適化132.690 sで、以前の一次map探索より短い。

### 電流自由度の修正

平均一致を廃止せず互換modeとして残し、新しいFFAG Taylor経路では各active-setごとに
次の正規化Chebyshev問題を厳密に解くよう修正した。

```text
min_{alpha > 0} max_i |alpha y_i - target_i| / band_i
```

これは各観測の正負残差が作る affine envelope の交点を全て評価する1変数問題である。
最適active pairから `d alpha / d y` を区分解析的に求め、
`d(alpha y)/dy` をEarlyTimes Jacobianへ合成する。ランダム200問題で独立LPとの
source-scale最大絶対差は `7.99e-15` だった。

### 回帰と次の実計算

- Chebyshev値・解析勾配、mean-mode互換、FFAG融合: 6/6 passed
- graph-front/beam、FFAG、accelerator既存回帰を含むfocused suite: 24/24 passed
- Taylor/isochronous側の追加focused suite: 64/64 passed
- `py_compile` と対象差分の `git diff --check`: 合格（既存LF/CRLF warningのみ）

Chebyshev版42k A/Bも、その後残留processが自然終了してから`mdx`で完走した。

- 736 HEX baseline: source scale `6.02911922`、実効電流1.50728 MA/coil。
  両運動量のactive dipole誤差をともに`37.5724151`へ均衡させた。
- 形状変更なしで旧一次map incumbent `37.5821923`を`37.5724151`へ0.0260 %更新した。
- EarlyTimes `R` ratiosは`[19.8189, 7.08317]`、監視したloose-band `T` ratiosは
  `1.21e-7`、`1.41e-7`。したがって今回も支配誤差はdipole profileである。
- graph-front 4、exact beam 2x2の1反復は完全solve 18本を実施したが、全候補が
  incumbentを改善せず、`exact_nonmonotone_beam_exhausted`で736 HEXを保持した。
  最良の探索中候補も37.57613であり、accepted historyは空である。
- 独立のbaseline solveと探索開始solveの線形solver許容誤差により、探索側incumbentは
  `37.5724499`（baseline比`3.48e-5`）だった。最終値はこれと同一で、形状悪化を
  受理したのではない。
- corrected run: ChargeGram 65.324 s、観測行27.818 s、探索400.998 s、
  同一process合計513.466 s、peak working set 1.206 GiB。

結論として、EarlyTimes接続と正しいsource eliminationは完成し、形状変更なしの
minimax値をわずかに改善した。しかし現在の736 HEXはgraph-front 4 / beam 2x2では
離散局所停留しており、「EarlyTimesへ替えるだけで大幅低減」は起きていない。
次は同じChebyshev目的のcluster-front多様性を増やすか、深さ3以上のbarrier探索を
ChargeGram再利用process内で比較し、単にfull solve本数を増やす前に候補responseの
独立rankと重複率を記録する。

### response-diverse cluster-front と depth-3 探索

上記の局所停留に対し、単純にexact solve本数を増やすのではなく、graph-front候補を
band正規化した応答変化で比較する診断・選抜を実装した。

- 候補poolとexact solveへ渡す選抜集合について、SVD数値rank、絶対相関0.995以上の
  重複pair率、最大絶対相関、最小noveltyを記録する。
- 最良の予測候補は必ず保持し、それ以降を「予測改善量」と既選抜応答部分空間からの
  noveltyでgreedy選抜する。重み0は従来のminimax順位を完全に保持し、今回の比較だけ
  0.5を用いる。
- `HDivMMMGenerationResult.graph_front_diagnostics`から各iteration/depthのpoolと選抜後
  のrank・重複率を取得できる。既存のtuple返却APIと既定挙動は変更していない。
- 42k比較driverはgraph-front 4、response novelty 0.5、exact beam width 3 / depth 3、
  barrier 1 %、完全active-set solve最大6候補として準備した。baselineと探索で同じ
  ChargeGram・観測行を再利用し、診断値もJSONへ保存する。

独立方向を含む4候補の回帰では、予測順位だけなら同一直線上の候補を選ぶ条件で、
novelty選抜は最良候補と直交候補を選び、pool rank 2、pool重複pair率0.5から、選抜後
rank 2、重複pair率0へ改善した。graph-front、Taylor融合、FFAG、accelerator既存回帰を
含むfocused suiteは33/33 passed、`py_compile`と`git diff --check`も合格した。

42,480 DoF本計算は、`mdx`の長時間IH validation終了を待たず、接続復帰後の空いた
`hibino`で前景実行した。1180 HEX、42,480 BDM1 DoF、初期736 active HEX、空気体積
要素0である。

- baseline最大band比は`37.5724240`、source scaleは`6.02890048`、実効電流は
  1.50723 MA/coilだった。
- depth 1で評価した5候補はすべて悪化した。最終的に採用した枝も一旦
  `37.5724291 -> 37.5794528`（+0.0187 %）へ悪化した。
- その枝からのdepth 2で、追加`[116,117,452]`、削除`[1111,1112,1136]`の交換を発見し、
  完全solve値を`37.5634286`へ低下させた。baseline比の改善は`0.00899544`、0.02394 %で
  あり、active HEX数は736のまま維持した。
- 予測値は`37.5483635`で、実改善/予測改善は0.3739だった。有限差分はoptimizer内で
  使用していない。
- graph-front depth 0では181 proposal、応答rank 9、重複pair率0.2568から4 proposal、
  rank 4へ選抜した。depth 1では167 proposal、rank 12、重複率0.0726から4 proposal、
  rank 4、重複率0へ選抜した。
- incumbentを含む完全solveは12本、各106--116反復、15.52--16.30 s。探索253.495 s、
  ChargeGram 63.205 s、観測行27.967 s、同一process合計362.968 s、peak working set
  1.203 GiBだった。
- `R`比は`[19.7810,7.08439]`、loose-band `T`比は約`1.2e-7`であり、支配誤差は依然
  dipole profileである。1 material iteration終了時点なのでconvergedではなく、
  stop reasonは`exact_nonmonotone_beam_budget_exhausted`である。

したがってresponse多様化付き非単調beamは、従来のdepth 1局所停留を実問題で越えた。
ただし0.024 %はPoCとしての探索成立であり、任意transfer matrix再現の達成ではない。
次はこのactive setをrestartし、同じH行列を再利用する複数material iterationへ進める。
同時にnovelty weight 0のdepth-3対照を取り、改善を深さと候補多様性に分離して評価する。

### depth-3対照と複数material iteration

`hibino`でnovelty weightだけを0.5から0へ変えたdepth-3対照を同一条件で実施した。
pure-ranked側でもdepth 1の悪化を越えたdepth 2で、追加`[96,97,100]`、削除
`[1111,1112,1136]`を発見した。

- novelty 0.5: `37.5724240 -> 37.5634286`、0.02394 %改善
- novelty 0.0: `37.5724478 -> 37.5603611`、0.03217 %改善
- novelty 0.0の選抜応答rankはdepth 0で3、depth 1で2、重複pair率はいずれも1.0だった。
  novelty 0.5はrank 4、depth 1の重複率0だったが、1反復の最終目的はpure-rankedの方が
  良かった。

したがって今回の局所停留脱出には探索深さが本質であり、response noveltyは候補rankを
上げるが、直ちに目的値を良くするとは限らない。pure-ranked結果を明示active-setでrestart
するloaderをscratch driverに追加し、source scale、mesh element数、BDM1 DoF、active数、
fixed return-yoke包含を検査してから、同一processで3 material iterationを実施した。

- restart baseline: `37.5603423`
- iteration 0: 追加`[93,1101,1102]`、削除`[90]`、`37.3884959`
- iteration 1: 追加`[1111,1112]`、削除`[682,793]`、`37.3665347`
- iteration 2: 追加`[90,444]`、削除なし、`37.3565769`
- この段階の改善は0.54250 %、元の`37.5724240`からの累積改善は0.57448 %。
  active HEXは736から740になった。
- source scaleは`6.0559660 -> 4.43495094`、実効電流は1.10874 MA/coilになった。
- 完全solveはincumbentを含む15本、探索328.965 s、ChargeGram 62.498 s、観測行
  28.904 s、同一process合計438.287 s、peak working set 1.203 GiBだった。

ただし目的の支配項は依然dipole profileである。normal-dipole比は`37.3566`へ下がったが、
低エネルギー側`R`比は`19.8667 -> 22.2073`へ悪化した。すなわち離散探索自体は実問題で
連続改善するようになった一方、任意transfer matrix再現へ進むには、dipole/bendを許容
制約に置き、`R`（後に`T`）を優先する階層minimax acceptanceへ目的を改める必要がある。

### transfer matrix優先の階層minimax

上記の診断に従い、Taylor-map目的を`normal_dipole`、`R`、`T`の行群として選択できる
ようにした。`primary_response_groups=("R",)`なら、ACA--QR--TSVD、LP、完全solve後の
採否は`R`だけを主目的とする。一方、`maximum_group_band_ratios`は完全active-set solveと
source再較正の後にだけ評価する厳密な物理guardである。線形化予測でguard合格とは判定
しない。

今回の42,480 DoF再開計算では、直前形状のnormal-dipole最大band比`37.3565769`に
0.01 %だけ余裕を与え、`37.3603126`以下を保ちながら`R`最大band比を最小化する設定と
した。guardに違反した完全solve状態は履歴にもnonmonotone beam parentにも残さない。
初期状態自体がguard違反なら即座に失敗させるため、目的の取り違えを黙って続行しない。

`MultiMomentumSecondOrderTaylorMapObjective`の行群抽出・群別band比、融合APIの主目的行、
解析Jacobian切出し、完全solve guard、beam探索でのguard排除を回帰に追加した。FFAG、
graph-front、accelerator magnetと対象beam探索を含むfocused suiteは33/33 passedし、
Python構文検査も合格した。実規模driverは計算用ホストへ配備してimportと出力非存在まで
確認済みである。両計算ホストが既存の長時間validationを実行中だったため、本節時点では
42,480 DoFの数値結果はまだ出ておらず、達成を主張しない。

## 2026-08-14 Lie map 4次・f5・Bishop/RMF接続

固定次数を一段進め、factorial Taylor map

```text
z_out = R z + T[z,z]/2 + U[z,z,z]/6 + V[z,z,z,z]/24 + O(z^5)
```

と Dragt--Finn 分解

```text
M = R o exp(:f3:) o exp(:f4:) o exp(:f5:) + O(z^5)
```

を `src/radia/accelerator_lie_topopt.py` に実装した。rank-five `V` とその全7多極
成分に対するforward ADを4次変分方程式で伝播し、`f3` の3重自己cascadeおよび
順序付き `f3`--`f4` crossを差し引いて `f5` を抽出する。generator全添字対称性、
`R/T/U/V`再構成誤差、Jacobian 3次係数までのformal symplectic residualをgateにした。
有限振幅評価は `f5 -> f4 -> f3 -> R` のimplicit-midpoint Hamiltonian flowであり、
評価map自体もsymplecticである。

`PlanarFourthOrderLieMapObjective`、局所TSVD reachable/unreachable証明、minimax field
correction、ACA--thin-QR--TSVD材料逆解析、whole-element HDiv-MMM topology gateまで
`V`を含めて接続した。これにより2次・x-y crossだけでなく、octupoleと低次cascadeが
作る4次x-y/chromatic crossを選択行として制御できる。Mathematica oracleも独立な
`f5` kickと4次map係数まで拡張し、生成JSONを更新した。

基準座標系については、全磁場を最初に
`recover_periodic_planar_closed_orbit` のDOP853で追跡し、得られた位置・単位接線を
`PlanarDesignOrbit` とする既存経路を確認した。新しい
`fourth_order_lie_map_from_tracked_orbit` は、そのRK軌道から平面Bishop/RMF moving
frameを作り、各断面のnormal/skew多極をdecapoleまでfitしてLie engineへ渡す。
したがって最初のRKは基準軌道と作業系の取得、後段の変分RKはその作業系内の偏差map
伝播であり、役割を分離した。

移動作業系は先行研究へ統一した。beam native adapterの従来実装は`parallel transport`
と表示しつつ、前の横軸を次の法平面へ射影する方式だった。これをBishop frameの
離散実装であるWang--Juttler--Zheng--Liu (ACM TOG 2008)のdouble-reflection RMFへ
置き換えた。軌道を微分してFrenet normalを作らないためゼロ曲率点を通過でき、離散
曲線に対して4次精度で不要な接線周りrollを抑える。平面問題では固定binormal方式と
入口frameの定数回転を除いて一致する。完全な非平面閉軌道用には、一周holonomyを
測定し、その逆rollを弦長に比例して配分する`periodic_frame`も実装した。これは
Farouki--Moonの境界条件付きconstant-twist minimal-twist frameに対応する。通常の
open-path RMFとは明示的に分離し、対称性縮約cellを単純なlast-to-first chordとして
誤って閉じない契約にした。

検証は、らせん軌道について独立に転記したWangのdouble-reflectionとの一致、非平面
閉曲線について残留holonomyの補正と始終端frameの閉包、RK軌道からLie mapまでを含む
focused Python試験25件、C++ beam transfer/dynamics試験、およびMATLAB/Python parity
manifest試験を通過した。`Build.ps1 -Verbose`によるMSVC/MKL/NGSolveビルドと通常の
`import radia`も成功した。

その後、同じ固定4次map milestone内でbody Hamiltonianを`H5`まで延長した。
Mathematicaで平方根・有限速度参照項の5次係数を導出し、native C++の
`HamiltonianJet6`へsymmetric `H5`と`F4=JH5`を追加した。normal/skew decapoleを加えた
9成分入力、pybind、standalone MEX、MATLAB wrapper、9点moving-frame調和fitの4次mode、
HDiv観測行まで同期した。これにより`V/f5`は低次cascadeだけでなくdirect `H5`
運動学項とdirect decapoleを含む。非展開parent Hamiltonianに対するtrajectory誤差は
振幅半減で約32倍減少する。現在の境界はlongitudinal fringe/edge vector potential、
非平面torsion、および任意次数normal formである。

## 2026-08-14 デカップル一次mapのKAN目標とrank-6 continuation

「任意のtransfer matrix」を物理的に許される範囲へ限定するため、各横方向2x2
symplectic blockをIwasawa/KANの3実数`(rotation, log_scale, shear)`で表す
`Symplectic2x2KAN`を追加した。この表現は実`Sp(2,R)=SL(2,R)`全体を覆う。
`DecoupledFirstOrderTarget`は

```text
diag(Rx, Ry, [[1, R56], [0, 1]])
```

だけを受理し、非symplectic横block、x-y-s coupling、静磁場で作れない縦方向energy
kickを入力時に拒否する。任意の縦方向`Sp(2,R)`はRF/時間依存電場を追加するまで対象外
である。

`certify_decoupled_first_order_reachability`は冗長な8個のABCD成分ではなく、横KANの
独立6座標に対するforward-AD JacobianをTSVDする。数値試験では全drift seedがrank 3、
一般の8区間normal-quadrupole seedがrank 6になり、KAN Jacobianは中心差分とも一致した。
`solve_decoupled_first_order_continuation`はsymplecticなKAN目標を段階化し、各非線形段で
横rank 6、目標残差、x-y-s decouplingを同時gateする。rankが落ちる段、R56が宣言basis
から届かない段、最小stepでも収束しない段は成功扱いにしない。rank-6 seed近傍の乱数
目標2件を通したが、これは監査可能なlocal-to-global continuationであり、遠方の全目標
に対する無条件な大域可制御性の証明ではない。関連Python回帰113件とparity manifest
試験を通過した。MATLAB fallback入口と試験も同期し、MATLAB R2026a Update 3へ公式
MATLAB MCPで接続して `test_python_fallback.m` の12件を実行した結果、12 passed、
0 failed、0 incomplete（6.4474秒）だった。新規wrapperのCode Analyzerは0 issueである。

## 2026-08-15 EarlyTimes の標準入力を HCurl ベクトルポテンシャルへ変更

EarlyTimes が受け取る実現場を、実数3成分の NGSolve
`GridFunction(HCurl(mesh, order=p))` に統一した。GridFunctionの値は磁気ベクトル
ポテンシャル `A`（T m）であり、C++ adapterはNGSolve自身の
`GridFunctionCoefficientFunction::Deriv()`、すなわちHCurl native `curl(A)`を
磁束密度`B`（T）として各追跡段・各moving-frame観測点で評価する。Radia側で高次
基底、要素orientation、Piola写像、点探索を再実装せず、Lie/A経路では`curl(A)`の
HDiv投影やregular-grid field mapも作らない。独立B-RKのHDiv入力は次節の更新を正とする。
次数`p`は保持されたFESpaceから取得して結果へ記録するため、
重複するorder引数は追加していない。

公開契約は次のようにした。

- Pythonの`GridFunctionField`、`propagate_grid_function_linear_map`、
  `propagate_grid_function_multipole_map`はHCurl vector potentialを既定入力とする。
- LIE bridgeの`fourth_order_lie_map_from_tracked_orbit`はHCurl入力専用で、
  normal/skew multipoleをdecapoleまでfitして`H2/H3/H4/H5`へ渡す。
- MATLABの`propagateGridFunctionLinearMap`、
  `propagateGridFunctionMultipoleMap`、`trackGridFunction`も
  `FieldRepresentation="hcurl_vector_potential"`を既定値にした。
- この時点のHDiv/VectorH1直接`B`互換経路は、次節でHDiv GridFunction専用B-RKへ
  置き換えた。生のB CoefficientFunctionは射影診断以外の追跡入力にしない。
- 結果には`field_representation`、`magnetic_evaluation`、
  `grid_function_space_class`、`grid_function_space_order`を含める。
  非HCurl GridFunctionを標準経路へ渡すとfail-loudする。

MSVC/NGSolveでpybind本体を再コンパイル・リンクし、更新バイナリの隔離試験で
GridFunction focused testsは14/14 passed、LIE/Taylor/topology focused testsは
109/109 passedだった。4次HCurl多項式`A`からnative curlで回収したnormal multipoleは
`[0, 2.4, 15, -80]`、skewは`[0, -0.6, -4, 20]`、最大fit residualは
`1.33e-14`、追跡LIE mapのformal symplectic residualは`1.47e-14`だった。
standalone MEXも再ビルドし、MATLAB R2026a Update 3でGridFunction beam testsは
6/6 passed、変更wrapperと試験のCode Analyzerは0 issueだった。

起動中MATLABのInProcess Pythonが旧`_radia_pybind.pyd`を保持しているため、
リンク済み更新DLLの`src/radia`への最終コピーだけはMATLAB終了後に行う。MEX配置と
隔離Python数値検証は完了しており、ここは数値実装ではなくWindowsのロード済みDLL
置換制約である。

## 2026-08-16 B-RKをHDiv GridFunction入力へ変更

B-RKの正式入力を、HDiv-MMM／`rad.Fld`が生成する連続B sourceからloft-chain meshへ
射影した`GridFunction(HDiv(mesh, order=4))`へ変更した。A/Lie側は従来どおり同じmeshの
`GridFunction(HCurl(mesh, order=5))`を使う。公開入口
`track_hdiv_b_map_cartesian_s`、`compare_hcurl_a_map_to_hdiv_b_map_rk`とそのbatch版を追加し、
生のB `CoefficientFunction`、HCurl以外のA、HDiv以外のB、異なるmeshのA/Bをfail-loudする。
Lie誤差三角形とp=5開口証明もHDiv-B入力へ統一した。生B追跡はsource-to-HDiv差を測る
診断にだけ残し、証明経路には使わない。

Cartesian B-RKは粒子path lengthで出口面イベントを探す方式を廃止し、design orbitの
`s`を独立変数にした。状態は各loft断面の`(x,y)`、グローバル単位接線、粒子path length
であり、場の評価位置を常に`R(s,x,y)`から構成する。このためadaptive RKの試行stageが
出口HEXの外へ出ず、HDiv GridFunctionを外挿しない。直線・曲線・4次Lie誤差三角形と
p=5開口証明を含むfocused回帰は合格した。

median-plane対称性について、HDiv-MMMのB/H sourceを鏡映点でもnative評価し、磁場を
軸性ベクトルとして`det(R) R B = -R B`で変換する
`FieldCoefficientFromSolution(..., reflection_normal=...)`を実装した。coilの`rad.Fld`
成分にも同じ軸性変換を適用し、そのparity-conditioned B sourceをHDiv p4へ射影する。
native C++/pybindをclean buildし、軸性鏡映の解析式、HDiv-B RK、未射影B拒否を含む
focused回帰を通した。

C型磁石q20の代表軌道検証（iron 474要素、HDiv-MMM 3396 DoF、beam 128 HEX）では、
HCurl p5／HDiv p4、対称化A/B source、`s`独立変数RKを用いて次を得た。

- raw B sourceの対称化補正最大値: `2.5196136e-3 T`
- 対称化B sourceと独立な対称化点評価の最大差: `2.9143e-16 T`
- HDiv p4と対称化B sourceの中点最大差: `8.22861e-5 T`
- median plane上のHDiv禁制成分最大値: 約`7.3e-16 T`
- HDiv-B RKと同じ対称化source-B RKの出口最大差: `1.80949e-8`
- HCurl-A RKとHDiv-B RKの出口最大差: `8.57505e-7`
- B-RK出口面残差: `1.586e-18 m`

対称化前のHCurl-A／HDiv-B差は`2.29132e-6`だったため、差の主要因の一つはHDiv p4
そのものではなくB sourceのreflection parityだった。対称化後も残る`8.58e-7`は、
Aのsource積分・ゲージ・HCurl投影とBのHDiv投影、および両Hamiltonian/Lorentz経路の差を
含む。次の収束調査ではA積分次数、HCurl p、HDiv p、loft断面幅とs分割を独立に振り、
この残差をLie truncationの前に詰める。

## 2026-08-16 HDiv B／curl(HCurl A)／rad.Fld B の3者比較を先行

A-RKとA-Lieの比較へ進む前提を明確化し、同一のloft-chain内グローバル点で次の3場を
直接比較する`compare_earlytimes_b_representations`を追加した。

1. `GridFunction(HDiv(mesh, order=4))`のB
2. NGSolve native `curl(GridFunction(HCurl(mesh, order=5)) A)`
3. 上下対称化したHDiv-MMM磁化場とcoil `rad.Fld`を直接評価するsource B

`curl(A)`はこのfield-boundary診断にだけ使用する。Lie mapは引き続きAそのものを
Hamiltonianへ入れ、B-RKはHDiv B-mapを使う。比較結果は全点の3場、全pair差、成分別最大値、
vector norm最大値を保存する。解析多項式場では3者とも`2e-12 T`以内で一致した。

C型磁石の45点（長手5、左右3、下／中央／上3）比較をmdxで実行した。HCurl p5、HDiv p4、
iron 474要素、beam 128 HEXは固定し、A sourceのtarget-adaptive最大積分次数だけを変えた。

- HDiv Bとrad.Fld source B: q14/q20/q24すべて`1.24081e-5 T`
- curl(HCurl A)とsource B: q14 `5.51126e-2 T`
- 同 q20: `1.82226e-2 T`（q14から66.9 %低下）
- 同 q24: `1.01107e-2 T`（q20から44.5 %低下）
- median planeのcurl(A)-source最大norm: q14 `9.91268e-3 T`、
  q20 `2.08336e-3 T`、q24 `6.79761e-4 T`
- q24でも上下点を含む最大normは`1.01107e-2 T`であり、22 source要素が最大積分次数24に
  到達したままである。

したがって、HDiv B-mapの射影はすでにsource Bと`1e-5 T`級で整合する一方、HCurl A-mapは
まだfield-boundary認定に達していない。q上昇で大きく改善するため現段階の主因はA source
積分の未収束だが、十分高いqでのHCurl p／mesh floorはまだ分離できていない。この3者差が
所定bandへ収束するまでA-RK対A-Lieの実磁石認定は保留する。

A-only比較API自体は実装済みで、Dragt--Finn因子を適用するsymplectic Lie state、直接
`R/T/U/V`を評価する4次Taylor state、未展開HamiltonianのA-RK stateを別々に返す。
また曲線RMFでは`e_x=bend_axis x tangent`に対してmetricが`1-h*x`となるようLie側の接続曲率を
`-h`へ修正し、曲線軌道回帰を追加した。追跡design orbitはB/ODE中点のcollocated `h(s)`を
明示保持でき、端点接線から得る幾何平均曲率は独立診断として残す。ただし、これらを使った
C型A-RK/A-Lie数値の採否は上記3者field gate通過後に行う。

### p=6と20 mm apertureの扱い

p=6を一方向の全40 mmに1要素で張る方針は採用しない。設計対応範囲を
`x,y in [-20,20] mm`とし、4本のx macro-stripをそれぞれ均等2分割、yを対称な
9層にするhpメッシュを追加した。長手1cellあたり72 HEX、局所の最大幅は
x方5.0 mm、y方4.44 mmである。`x=0`は設計軌道を含む内部face、`y=0`は
中央層の要素内部のままであり、HCurlの接線traceとmedian-planeのポリシーを保つ。

q24のp=6収束調査では、y=1層から3層への細分化によりHDiv B-source B差は
`1.24081e-5 T`から`7.47395e-7 T`へ改善した。一方、curl(HCurl A)-source B差は
`1.29161e-2 T`から`1.27664e-2 T`への小改善に留まり、p5/q24の`1.01107e-2 T`よりも
大きい。したがってp=6化だけで広いapertureの精度を主張せず、先にA source積分と
hpメッシュの3者field gateを収束させる。±20 mmのHCurl空間生成は回帰試験に
通過したが、実C型場の±20 mm精度認定は未実施である。
旧C型fixtureの磁極ギャップは全10 mmのため、この形状のまま±20 mmメッシュを
延ばすのは物理的でない。過去のq14/q20/q24結果の再現用に旧既定値は残し、
`--wide-20mm`で全50 mmギャップ、60 mm磁極幅、±20 mmビームメッシュ、p=6、
8 x strips×9 y layers、評価範囲±18 mmを一括選択する物理C型プリセットを追加した。

## 2026-08-16 HCurl A-mapのsourceを厳密な等価電流解析核へ

前節でq24でも`1e-2 T`級に留まっていたcurl(HCurl A)-source B差の原因を切り分け、
根治した。切り分けの要点は、AをHCurlへ射影する**前**の生のA CoefficientFunctionを
中心差分でcurlしてsource Bと比較したことである。同じC型45点で

- 生curl(A) - source B: q14 `3.9456e-2`、q20 `1.6342e-2`、q24 `1.1071e-2 T`
- curl(HCurl p5 A) - source B: q24 `8.5400e-3 T`
- HCurl射影のみの寄与: q24 `4.5719e-3 T`

であり、誤差は射影前から同じ大きさで存在した。すなわち**HCurl p=5射影は無罪**で、
原因はA sourceそのものだった。

HDiv-MMMの場評価層は**Bだけが厳密解析核**(`TetVolFieldCubic`/`QuadTriField`)で
積分され、**Aだけが点ダイポール数値積分**という非対称を抱えていた。ビーム管は鉄要素
(30 mm)から1.5 mmしか離れておらず、その要素の深いnear fieldなので`1/R^2`核に対して
求積次数を上げても代数的にしか収束しない。定数Mへ潰して細分する案も否定した。
レベル0/1/2で厳密BDM1 Bとの差は`0.103/0.142/0.140 T`、ギャップ中心で相対26 %で、
鉄要素≫標的距離のため`h ≪ 1.5 mm`すなわち約900万sub-tetを要する。AをBから作る案は
`LIE_MAP.md`のA/B独立性契約を壊すため不可である。

そこで等価電流の恒等式
`A = mu0/(4 pi) [ INT_V (curl M)/R dV + INT_dV (M x n)/R dS ]`
を要素ごとに厳密核で閉じた。BDM1なら`curl M`は要素内定数、面電流`M x n`はアフィン
なので、既存の`rad_hdiv::PhiTet`/`TriPotential`/`TriMoment1`で閉形式になる。内部面は
両側とも残す。HDivは`M.n`連続だが接線成分は跳び、その跳びがシート電流そのものだから
である。反射対称化では**Aは極性ベクトル**として`A_sym(r)=0.5[A(r)+R A(R r)]`とし、
等価電流も極性として変換する。既存コードがcoil Bを`0.5*(b-reflected_b)`、coil Aを
`0.5*(a+reflected_a)`としているのと整合する。

- C++: `radia::ngsolve_bridge::HDivExactVectorPotentialCoefficient`
  (`src/core/rad_ngsolve_field_coefficients.h`)とpybind
  `_HDivExactVectorPotentialCoefficient`
- Python: `vim.VectorPotentialCoefficientFromSolution(res, construction=...)`。
  `auto`はorder-1の四面体HDiv空間で`exact`を選ぶ。曲がった四面体と非アフィン(BDM2)は
  fail-loudする。実際に選ばれた構成は必ずstatsへ記録する。
- 検証スクリプトに`--a-construction exact|quadrature`を追加し、過去のq14/q20/q24も
  再現可能なまま残した。

検証オラクルは4系統で、いずれも合格した。定数MはRadia自身の解析`ObjTetrahedron` Aと
相対`1e-12`以下(数値積分版は`2e-7`)、アフィンMは遠方の高次求積と相対`1e-9`以下、
near fieldでは`curl A`が厳密電荷核Hと相対`1e-5`(中心差分律速)、および反射対称化の
恒等式と非アフィン空間のfail-loudである。

**修正後のC型3者field gate**(45点、|B|最大`0.390 T`、HCurl p5/HDiv p4)

| beam loft chain | HEX | HDiv B - source B | curl(HCurl A) - source B |
|---|---|---|---|
| 1層 / 5.0 mm / hsub1 | 128 | `5.26e-5 T` | `1.18e-5 T` |
| 3層 / 5.0 mm / hsub1 | 384 | `1.40e-6 T` | `2.31e-6 T` |
| 5層 / 2.5 mm / hsub2 | 2560 | `1.53e-7 T` | `5.06e-8 T` |

curve orderを2から4へ上げても両列とも完全に不変であり、この軌道では幾何次数が律速で
ないことも確認した(縦1cellあたりのsagittaは`1e-7 m`)。生Aの差分curlは
`1.43e-6 T`(h=4e-5)から`3.58e-7 T`(h=2e-5)へ厳密に2次で落ち、しかもビームメッシュに
一切依存しない。つまりこの残差はプローブ側の打切りであってsource誤差ではない。最細
メッシュでは射影場の一致(`5.06e-8`)が差分プローブの打切り(`3.58e-7`)より小さく、
差分では測れない領域に入っている。

なお、`project_design_orbit_gauge`のゲージ残差は128 HEXで`8.2375e-6`、鉛直3層でも
`8.2501e-6 T*m`とほぼ不変であった。横方向解像度律速ではないため、縦方向と水平方向を
含めた細分で対処する。

## 2026-08-16 設計軌道ゲージをRBFコロケーションから離散KKTへ置換

上記のゲージ端点残差を追跡し、機構を3段階の実測で確定してから
`project_design_orbit_gauge`を書き換えた（菅原の指示「HCurlに射影してから
ゲージング」「grad(V), V∈H1で固定」に基づく）。

**旧RBFの端点異常の正体**: 入口・出口のstation残差`8.2e-6 T*m`は、内側へ
ずらすと消えるどころか境界要素内で`±3e-4 T*m`の振動を示した（station値は
ゼロ交差近傍の偶然）。原因はRBF基底の片側支持→解析χのH1射影欠陥→p=5基底の
リンギング。メッシュ細分（6倍）・RBF幅・station数のいずれにも不応答であること
を個別に実測して棄却した。正準運動量換算でδp/p~1e-4の実害があり、測定対象の
軌跡差8.6e-7より大きかった。

**離散KKTゲージの設計と、途中で確定した3つの負の結果**:

1. ゲージ未知数を`V∈H1(p+1)`に取り、de Rham包含`grad(H1_{p+1})⊂HCurl_p`で
   `A_h+grad V`を構成的にHCurl(p)の元とする（ゲージ後の再射影が消滅、
   curl変化は`3e-15`）。ただし**軌道station 33点の点拘束だけでは不十分**:
   stationで機械精度`1e-14`を達成しながら、997点密監査では`8.6e-3`
   （ゲージ前と同等＝実質無効）。勾配の点評価はH1上の有界汎関数でないため、
   最小化はstation間を野放しにする。拘束を129点に増やすと干渉して`8.1e-2`へ
   悪化する。
2. 拘束を401点まで密にすると曲線を張り密監査`2.8e-11`に達するが、Schur補元の
   条件数が`7.6e19`（特異）。**TSVD（rcond=1e-12）**で解くと保持ランク
   511/802、密監査`1.4e-12`、`|V|`はrcondに不感（スパイク解でない）。
3. 等方`min|grad V|²`は曲線拘束を「横に変化する」最安の解で満たし、アパーチャ
   全体の`A_x=0`ゲージを`|A_x|=4.6e-2 T*m`まで破壊した。`A_x=0`がゲージ
   ポテンシャルのx依存性を先に固定しているため、軌道ゲージは残りの(y,s)自由度
   だけを使うべきである。局所x微分への**異方罰則**（`axial_leakage_penalty`）
   で漏れは1/罰則スケール: 1e6で`1.46e-6`（A-RKのA_xゲート1e-6を僅かに超過）、
   1e8で合格。旧RBFは基底を局所x線に沿って一定に設計して同じ制約を暗黙に
   課していた。

**検証契約の修復**: 旧ゲートは「拘束を課したstationでしか残差を測らない」ため、
station間で無効なゲージでも合格し得た。新実装はゲートを**独立な監査フレーム**
（セグメントあたり31点、拘束の12点と互いに素）へ移し、
`maximum_orbit_gauge_residual_t_m`は監査値を返す。結果オブジェクトに
`constraint_count`/`schur_condition`/`schur_retained_rank`/監査配列を追加した。
既存テスト`test_beam_curvilinear_mesh.py`は12/12合格。

**LIEのH1ゲートによる正しい検出**: 新ゲージでC型三角形検証を回すと、ゲージと
A-RK/B-RKトラックは通過し、LIE構築が`max|H1|=4.16e-6 > 3e-6`（x成分のみ）で
停止した。H1_xは軌道上の力平衡`-(h+da_s/dx)`であり、設計軌道をsource Bで
追跡する一方A-mapは3層メッシュのHCurl射影（場誤差`2.31e-6 T`）を使うため、
その双極子ずれを正しく検出している。ゲージでなく場の射影収束の問題であり、
収束済みの5層/2.5mm/hsub2メッシュ（場誤差`5.06e-8 T`）で再実行する。

**median-plane対称性証明の失敗と真犯人**: 細分メッシュではLIEのparity証明が
`8.80e-6 > 3e-6`で停止した。TSVD雑音仮説はrcond掃引（1e-12..1e-6で解が完全に
同一、rank=385不変）で棄却。段階別のパリティ測定で真犯人を一意に特定した（後述のH1調査とは独立の問題）:

| 段階 | Ax_even | Ay_odd | As_even |
|---|---|---|---|
| source CF | 1.7e-15 | 3.4e-16 | 7.0e-16 |
| axial gauge後 | 4.9e-10 | 4.96e-7 | **2.44e-6** |
| HCurl射影a_h | 2.8e-10 | 4.96e-7（不変） | 2.44e-6（**桁まで不変**） |
| KKTゲージ後 | 2.8e-10 | 7.1e-7 | 3.8e-7 |

ソースは厳密対称、**HCurl射影は対称メッシュ上でパリティを桁まで保存**（射影
演算子と鏡映の可換性の実証）、KKTはAs_evenをむしろ改善。壊していたのは検証
スクリプト内`project_axial_gauge`の**null空間正則化1e-12**である。χの(y,s)方向
は正則化項だけで支配されるため、荷重ベクトルの浮動小数点集積雑音epsが
null方向で`eps/1e-12=1e-4`相対まで増幅され、その∂/∂sがAs_evenを2.44e-6汚染
していた（DoFが増える細分メッシュで悪化する観測とも整合）。正則化を1e-6へ
上げると全段が清浄化した: axial 7.6e-9、a_h 9.0e-9、**KKTゲージ後3.3e-10**
（監査残差3.2e-13は機械精度のまま、A_xバイアスは1e-6相対で無害）。KKT自体は
入力が綺麗なら清浄で、以前の+2e-7のAy_oddは汚染されたv-target
（-A_y(y=0)≠0）への正当な応答だったことも確定した。

## 2026-08-17 LIE H1_x爆発の局在化: 境界面フィット汚染と実物の内部床の分離

パリティ修復後、細分メッシュのLIE構築は`max|H1|=3.35e-4`で停止した（3層では
1.0e-5）。axial正則化のスイープ（1e-12対1e-6でmaxH1が1.003e-5対1.043e-5）で
正則化は無罪と確定し、セグメント別のH1プロファイルで機構を分離した:

```
coarse(3層):  [9.6e-6 1.0e-5 | 内部 1.6e-7〜7.6e-6 | 1.0e-5 9.8e-6]
fine(5層/2.5mm/hsub2): [3.3e-4 1.1e-5 | 内部 3.0e-7〜7.7e-6 | 1.2e-5 6.2e-5]
```

**内部はメッシュ間でセグメント毎に~5%まで一致**（3.4対3.8、7.6対7.7、4.4対4.4
[1e-6]）。すなわち内部のH1床~8e-6は**メッシュ収束済みの実物**で、その正体は
degree-5横方向ジェット契約によるフリンジ場の多項式打切り（fitAs残差~7e-4 T*m
のx線形成分への漏れ）である。一方、**境界セグメント（入口・出口面上のフィット
断面）だけが細分で30倍悪化**する。境界面上の断面は離散場の片側微分content
（∂a_s/∂xと∂²V/∂s∂x）を最も制御の弱い場所でサンプルするためで、要素が薄く
なるほど境界層が濃くなる。1パス射影プローブのmaxH1=3.3475e-4が失敗した検証
実行と5桁一致したことは、`P(A+∇χ)=P(A)+∇χ`の恒等性の実測確認でもある。

**対策（field-mapの標準作法）**: 写像の認定区間を場マップ台の内側に取る。
`trim_orbit`を追加し、軌道を各側`--orbit-margin-stations`（既定2駅=5mm）だけ
延長して追跡（±45mm）、loft管は延長軌道から構築し、LIE/A-RK/B-RK/フィットは
内側33駅（±40mm、従来と同一間隔）の切り出し軌道を使う。境界汚染域は写像区間の
外に出る。H1許容は`--lie-reference-orbit-tolerance 2e-5`とした。定量的根拠:
H1~1e-5の双極子キックの出口座標への影響は`H1·L²/2≈3e-8 m`で、三角形の測定
対象（1e-6〜1e-7）より1.5桁下。内部床はジェット契約の実物であり、その写像への
影響はLIE−A-RK脚が正直に測る。

## 2026-08-16 gauged-LIE / ungauged-A-RK / HDiv-B-RK の三者比較層

検証スクリプトに`gauge_invariance_triangle`を追加した。正準運動量はゲージ依存
なので、各経路の出口状態を**その経路自身のA**で力学運動量へ変換して比較する:

1. gauged-A LIE（4次Dragt--Finn）
2. **ungauged**（axialゲージのみ）Aの厳密正準A-RK — 入口状態は
   「gauged正準→力学→ungauged正準」と換算してから追跡
3. HDiv B-map Cartesian B-RK

gauged-A-RKを橋として保持し、ungauged-A-RKとの力学差は正準機構全体の
**ゲージ不変性の内部検査**になる（χはδ非依存なのでellもゲージ不変で比較可能）。
LIE−A-RKはtruncation、A-RK−B-RKは場ルート差を分離する。

## 2026-08-17 三者トラック検証の完走（C型磁石、2880 HEX、pass）

厳密A source＋離散KKTゲージ＋パリティ清浄axial＋軌道マージンの最終構成で
三角形が完走した（margin 2駅、H1許容2e-5、実行118分）。力学変数での出口比較:

| 脚 | 値 | 解釈 |
|---|---|---|
| ゲージ不変性（ungauged−gauged A-RK） | **4.77e-10** | 入口変換・厳密Hamiltonian・出口変換・離散ゲージ鎖の全体が積分許容誤差レベルで検証された |
| A-RK − B-RK（独立場ルート） | **1.152e-6**（px支配） | HDiv-B対source-B追跡は**8.4e-12**でB側は実質厳密。残差はA側経路と区分一定h(s)の疑い |
| LIE − A-RK | **2.217e-4**（**py支配**） | 振幅truncation（z⁵~1e-19級）では説明不能。**piecewise-constant-in-s body multipole契約がフリンジのs依存（edge focusing）を表現できない**ことの実測で、LIE_MAP.mdが「Longitudinal fringe/edge vector potentials remain separate work」と宣言したスコープ境界そのもの。全長80mmの大半がフリンジのC型で支配的に見える |
| LIE − B-RK | 2.217e-4 | 上の合成 |

付随ゲート: ゲージ監査3.0e-13、maxAx 2.3e-7、ungauged maxAx 4.8e-7、
maxH1=7.55e-6（境界爆発3.3e-4はマージンで実物の内部床へ、設計通り）、
Lie因子vs直接R/T/U/V多項式 5.1e-13、シンプレクティック残差2.0e-10、
B-RK出口面残差2.9e-18。LIE−A-RK内訳=[x 9.8e-8, px 2.5e-6, y 8.7e-6,
**py 2.2e-4**, ell -1.7e-9]、A-RK−B-RK内訳=[x -4.6e-8, **px -1.2e-6**,
y 6.0e-10, py 4.9e-11, delta 0]。

LIE−A-RKのセグメント長1次スケーリング（フリンジ離散化）を確定するため、
軌道65駅（margin 4駅）の確認走行を追加実行した。

**スケーリング確認（s65 + 開口スイープ）で3脚すべての制御ノブが確定した:**

| 脚 | s33→s65（駅倍増） | 開口±8→±4mm | 確定した起源 |
|---|---|---|---|
| ゲージ不変性 | 4.8e-10→1.2e-10 | 不変 | 積分許容誤差（収束済み） |
| A-RK−B-RK | 1.15e-6→**5.4e-7（2.1×）** | **完全不変** | 区分一定h(s)の軌道離散化（Δsの1次）。駅数で制御 |
| LIE−A-RK | **2.22e-4→2.20e-4（不変）** | 1.10e-4→**5.0e-5（2.2×）** | **degree-5ジェット契約の開口打切り**。フィット残差fitAy（1.2e-4→1.5e-5）と強相関、セグメント分割と振幅（z⁵~1e-19）に不感 |

当初のフリンジs離散化仮説はs65で棄却された（LIE−A-RK不変）。なお粗メッシュの
LIE−A-RK（1.1e-4）が細メッシュ（2.2e-4）より小さいのは、忠実な場ほどジェットが
表現できない高次内容を多く含むためで、フィット打切り説と整合する
（fitAy: 粗1.2e-4対細5.3e-4）。

**結論**: 三者は「厳密に一致すべきもの（ゲージ不変性）は4.8e-10で一致」し、
独立場ルート差はΔs1次で5.4e-7まで、LIE truncationは宣言済みジェット契約の
開口打切りとして定量化された。LIE精度の改善路はジェット次数の引き上げ
（degree-6）であり、その拡張のための記号golden（一般ジェットH2..H6、W縮約、
f6回復契約）は同日のp=6 Mathematicaオラクルとして委託済み — 2つの作業が
ここで噛み合う。

## 2026-08-17 CanonicalHCurl の設計決定（次期表現、検証は再走）

三角形の帰結（LIE床=横ジェット開口/次数律速、y方向が最も厳しい、sは緩い、
FE空間でのゲージ数値化が苦労の源泉）を受け、追跡特化の場表現
**CanonicalHCurl** を設計した（命名含め菅原と合意）。

- **断面1要素×sチェーン**（スペクトル要素）。設計軌道が各要素の中心線を貫き、
  x=0/y=0とも要素内部になるため、4ストリップ構成のtrace単価性問題が消滅する。
  横方向分解能はhでなく異方次数 (p_x,p_y) で取る。
- **構造としての制約**（証明書でなく基底が持つ）: a_x≡0（成分DOFなし）、
  median-planeパリティ（a_s: y偶、a_y: y奇→軌道面で自動0）、中心ゲージ
  （a_sのx⁰y⁰sᵏモードを基底から除外→軌道上a_s≡0が恒等）、および
  **真空条件 curl curl A = 0**（ビーム管内はソースなし。菅原指示 08-17）。
- **真空条件の帰結**: パリティ展開の高次y係数が低次係数の(x,s)微分で決まる
  漸化式になり、**p_yは独立ノブでなく生成される**。自由データは実質midplane上の
  (x,s)関数＝多極子関数b_n(s)に縮退し、DOFは~1/p_yに潰れ、LIEの多極子規約へ
  s依存付きで直結する。LIE_MAP.mdの「測定midplaneからの継続禁止」とは別物
  （こちらは検証済み厳密Aを全3D点でフィットし、拘束は真の場が持つ性質。
  フィット残差が打切りの正直な尺度）。実装は(b)要素ごと厳密多項式curl-curl
  作用素の数値ヌル空間（曲率h込み、試作本命）と(a)Mathematica記号漸化式
  （p6オラクル流儀のgolden、次元数の独立照合）の両建て。交差空間
  （a_x≡0∩パリティ∩中心ゲージ∩真空）の次元一致がStage 1の最初の測定項目。
- **s方向はp_s次**: LIEジェットの区分0次凍結を解消する（要素内sポリノミアル
  Hamiltonian）。
- **多項式モーダル空間ではゲージが厳密係数演算に潰れる**: χ=-∫a_s ds-y·a_y型
  の補正が空間内で閉じた線形写像になり、RBF/KKT/TSVD/監査フレームの数値ゲージ
  機構は不要になる（今回のゲージ苦労はFE空間で数値的に課した代償だった）。
- 役割分担: NGSolve HCurl GridFunctionは**検証済み場境界**として残り
  （A-RK/B-RKの真値経路）、CanonicalHCurlはRadia所有の追跡・LIE消費層。
  評価は多項式（メッシュ探索なし・微分厳密・~µs/評価）。

**設計変更につき検証は再走**（合意済み）。再検証のオラクルは今回の産物が
そのまま務める: 厳密A源、B-RK、旧gauged A-RK、p6記号golden、三角形ハーネス。
段階: (1) 解析多項式場で基底+ゲージ手術の機械精度確認（C:\temp）、(2) C型で
射影+場ゲート+**要素間ジャンプ証明書**（断面1要素チェーン固有の新しい破れ方）
+(p_x,p_y,p_s)収束、(3) 三角形再走（新旧A-RK相互比較が新しい脚）、(4) LIEの
係数直読。現行4ストリップ経路は全ゲート通過までproductionを維持する。
関連probe: Grad(HCurl GF)は利用可で解析Jacobianと1.4e-11一致（既存A-RKの
差分床3.6e-7は即時削減可能）、loftの鏡映辺閉包は完全（0/286不一致）。

## 2026-08-16 任意p=6 HCurl A-mapのLIE-mapをMathematicaで導出

`lie_map_symbolic_oracle.wls`（body multipole特化）を一般化した
`validation_test/ffag_topopt/lie_map_symbolic_oracle_p6.wls`と委託golden
`lie_map_symbolic_reference_p6.json`を追加した。

- **一般ジェット**: median-planeパリティ（a_s: y偶15項、a_y: y奇12項、総次数
  ≤6）、計量`(1+hh x)`（hh=-h_orbit）、共変a_s、設計軌道条件`cs10=-hh`。
  厳密親HamiltonianをH2..H6へ記号展開（H6がp=6の新規部分: 次数6のa_s、
  py·a_y交差、a_y²）。H1≡0を記号的に確認。
- **変分ジェットによる写像縮約**: 固定軌道z=0まわりの変分方程式（5次まで）を
  WorkingPrecision 30で積分し、2つの委託方向に対する`R·u, T[u,u], U[u³],
  V[u⁴], W[u⁵]`をgolden化。Wは5次写像テンソルで、p=6ジェットが4次写像を
  超えて支えるものそのもの。
- **1自由度5次Dragt--Finnサンプル**: 既知のf3/f4/f5/f6から合成写像
  `exp(:f3:)∘exp(:f4:)∘exp(:f5:)∘exp(:f6:)`を次数5まで構成。将来の
  `dragt_finn_factorize_fifth_order`が委託f6を厳密回復すべき契約。
- **自己検査**: 多極子特殊化が既存4次オラクルの閉形式を再現、Rシンプレク
  ティック残差<1e-24、変分δ1=MatrixExp·u。
- **独立検証**: `tests/test_accelerator_lie_p6_oracle.py`（4/4合格）。解析
  スポットチェック（`H6_x⁶=-720·cs60=-261.818...`、`H2_y_py=-cy01=-2/9`）と、
  **打切りなし**の厳密Hamiltonianをscipy DOP853で追跡して
  `flow(εu)-Σεᵏ/k!·縮約ₖ`が`O(ε⁶)`で閉じること（実測比63.8/理論64;
  H7以上は5次写像に寄与しないため厳密流と一致すべきという構造を利用）。

Mathematicaは導出・golden専用でランタイム依存ではない（既存オラクルと同じ
方針）。JSON出力は機械倍精度に正規化した（任意精度ゼロ`0.e-29`が不正JSONに
なるWolfram RawJSONの罠を踏んだ）。

## 2026-08-17 CanonicalHCurl Stage 1: 次元法則 dim = p_x·(p_s+1) と p_y 生成則の実証

`C:\temp\earlytimes_a_source\probe_canonical_basis_stage1.py` +
`probe_canonical_px_calibration.py`（研究scratch）。初回実行は2つの既知教訓の
再演で異常値を出した:

1. **E1次元がp_yで飽和しない** → 正体は残留純ゲージ（`a_y`のx⁰モード =
   χ(y,s)ゲージ閉包そのもの、B=0）。構造ゲージを拡張し**gauge-rigid化**
   （a_yからx⁰モードを除外 + 中心ゲージ）で解消。
2. **E2収束が停滞**（易しい場でもrel 1.6e-4床） → 生メートル座標の単項式で
   x⁶~1e-13となるVandermonde病（frame-gauge ScaledFitと同じ教訓）。
   **方向別正規化座標**で基底を張り、真空作用素に異方スケール1/a²を入れて解消。

修正後の実証結果（すべて真空 curl curl A=0 + median-planeパリティ +
gauge-rigid空間の SVD null space）:

- **次元法則**: 7組すべてで **dim = p_x·(p_s+1)** が厳密成立
  （(6,0)→6, (6,1)→12, (6,2)→18, (6,3)→24, (6,5)→36, (8,1)→16, (8,3)→32）。
  = 空間はミッドプレーン多極プロファイル **b_m(s), m=1..p_x, s次数≤p_s** で
  厳密にパラメータ化される。b_n(s)スプライン級との対応が次元レベルで閉じた。
- **p_y は選ぶものではなく生成されるもの（実証）**: p_y上限を上げると次元は
  飽和。飽和深さは全テストで **p_y ≤ p_x+2**（ps=1では p_x ちょうど、
  ps≥2で+2止まり。ps=5でも+2）。実装はp_y=p_x+2を内部固定にしてよい
  （ユーザーノブにしない）。
- **p_x収束（合成場較正）**: 易しい真空場（線電流対 x0=±50mm）は
  ×40–140 / Δp_x=2 で px=12にてrel 5.9e-12。極近接の最悪ケース
  （線電流対 y0=±5mm、y比0.7）は×1.4–1.6 / Δp_x=2（Bernstein楕円律で説明
  可能: 特異点がbox外接楕円の内側）。実磁石はこの中間 → Stage 2で実測。
- 2D多極 m=1..4 は機械精度（<5e-17）で再現（回帰基準維持）。

設計帰結: 断面1要素・p_s=1でも **16 DOF/要素（p_x=8）** と極小 — Lie-map
向け。次段: 実C型HDiv-MMM場の射影でp_x実測 + B誤差ゲート + 要素間jump証明。

## 2026-08-17 CanonicalHCurl 設計裁定（菅原）: p_y打ち切り・p_s方針・全体積射影

Stage 1結果への菅原裁定3件と、その定量裏付け（`C:\temp\earlytimes_a_source\`）:

1. **p_y < p_x であるべき（アパーチャー異方性）** → 概念整理: p_yは次元に
   寄与しない（dim=p_x·(p_s+1)はp_y非依存 — y高次項は自由係数でなく漸化式で
   決まる従属係数、無駄DOFは厳密ゼロ）。ただし**評価コスト（単項式数）としての
   生成級数打ち切り p_y_trunc は正当**: 正規化単位で生成y項は(a_y/a_x)^j減衰
   （実機0.438）。実測（最悪ケース磁極5mm）: py≤4打ち切りの誤差増分~4%、
   **py≤6で<1%**。さらにLIEルートはジェット次数(5–6)超の総次数を読まないので
   **p_y_trunc=ジェット次数が自然な既定**。決定: ノブは(p_x,p_s)のみ、内部
   p_y_trunc=min(p_x,6)。
2. **p_sは0/1が基本、p_s≥2が使えればLIE mapとして新しい** → 同意+実現可能:
   p=6オラクル（08-16）でs依存Hamiltonianの変分ジェット+Dragt–Finn因子化が
   多項式で閉じることは検証済み。既定p_s=1、フリンジ用p_s=2–3をopt-in。
3. **midplane以外の場を再構築してはダメ、HDiv-MMMから全空間で係数を決める**
   → 全面同意=実装方針そのもの: 射影は**HDiv-MMM 3D場の全アパーチャー体積
   （y全域+sスラブ）B全成分の最小二乗**。b_m(s)は係数のラベルであって、値の
   決定は全体積フィット。midplane-only Taylor継続は不採用（EarlyTimes政策
   「measured median-plane Bのoff-plane continuation禁止」と同思想）。

**実C型 Stage 2 中間結果**（maxh=0.03粗メッシュ、中心断面）:
- 2D断面フィット: **px=6でrel 1.6e-3到達**（実機のx方向はE2a型の易しい側 —
  磁極が滑らかな面電荷なのでE2b線電流最悪ケースより大幅に有利）、以降~1e-3で
  プラトー。
- プラトーの主因を診断: 中心断面は装置のx鏡映対称面なので真のB_sは恒等0のはず
  だが実測|B_s|~3.5e-3 — **粗メッシュ(273要素)の非対称性ノイズ**。s非依存の
  y-odd B_sはMaxwell非整合なので真空基底が正しく拒否（無拘束フィットは拾う）
  = **真空射影がMaxwell整合フィルタとして機能している証拠**。メッシュ細分で
  ノイズ・プラトー同時降下を確認中（backgroundジョブ）。h=0直交フレーム近似の
  寄与~h·x·B~4e-4も併存（本実装のh≠0分母クリア作用素で除去予定）。

## 2026-08-17 CanonicalHCurl Stage 2: 曲率作用素・厳密オラクル・フレーム取り違え訂正

**h≠0 分母クリア真空作用素**（`build_space_curved`、研究scratch）: 計量
g=1+h̃x で curl curl A=0 を g 掛けで多項式恒等式化（C1=g∂y(gP)+∂sM,
C2=∂sN−g∂x(gP), C3=g∂xM−h̃M+g∂yN; N=g·Bx, −M=g·By, P=Bs）。
一様By場を曲率チャートで機械精度表現（7e-16）。

**Mathematica厳密オラクル**（ℚ上、`canonical_hcurl_dimension_oracle.wls`）:
- dim=p_x·(p_s+1) を8/8組で厳密確認。p_y飽和はpx+2✓（floatのpy=6での
  +1過大計上はsスケール不均衡のtol誤判定と特定 — fixed-dim設計では無害）。
- **曲率チャートの厳密kernelは3次元まで崩壊**（h≠0でほぼ全族のh継続が
  多項式打切り外のx^(px+1..2)項を要求 — 制約行フィルタでは救えない構造的
  事実）。⇒ 空間定義は**fixed-dim least-defect**（パリティ+ゲージ空間の
  真空欠損最小 p_x(p_s+1) 次元部分空間 = C(h)の最小特異ベクトル）が唯一
  正解。残余方向の欠損はO(h·打切り尾)~4e-5相対で設計通り。

**フレーム取り違え訂正（重要）**: `PlanarDesignOrbit.frame_at` は
**(horizontal, vertical, tangent)** を返す。Stage 2の初期スラブプローブは
(tangent,·,horizontal) と誤展開し**縦方向スライスをサンプル**していた
（「中心断面2D px=6でrel 1.6e-3」は無効）。物理サニティで確定: 修正後
フリンジ|B_s|=0.125 T = y·∂sByの期待値✓、中心断面|B_s|=4.9e-5✓鏡映対称。
教訓: フレームAPIは返り順をdocstringで確認してから展開（部分unpackの
「validation usageから推定」は危険）。

**正軸での実測（maxh=0.02, 784要素）**:
- 中心断面2D(ps=0): px=4以降 rel~1.1e-2 プラトー = **本物の3D効果**
  （対称断面でもΔ_tBy=−∂s²By≠0、見積り∂s²By·a²/4≈2.6e-3 Tと整合）。
  **中心部でもps≥1必須**の設計裏付け。
- フリンジ・スラブ（±5mm 1要素, px=8, curved基底）ps掃引:
  rel 4.9e-1(ps=0)→2.0e-1(1)→1.9e-1(2)→9.1e-2(3)→8.6e-2(4)。
  奇数次で効く（Enge型が磁極端についてほぼ反対称）。1要素フリンジrel 9%が
  出発点; 実用は2-3要素分割 or 高ps。
- 中心スラブでのflat vs curved差は源メッシュ誤差床(~4e-3, HDiv-MMM離散化
  誤差場=厳密curl-freeだが要素スケール短波長で低次基底が正しく弾く)に埋没。
  h効果(4e-4)の実場実証は源が細メッシュ収束してから。
- メッシュ非対称ノイズはmaxhに**非単調**（0.03:7.8e-3 / 0.02:1.3e-5 /
  0.015:1.0e-2、x鏡映欠損）— netgenメッシュのカオス性の再確認。

**eddy-bubble化（菅原質問への回答）**: 現構成はまさにそれ。gauge-rigid化で
勾配モード(B=0)を除去した後のnull space基底は各々「B≠0かつcurl B=0の
源なし渦胞(vacuum bubble)」。参照要素でbubble表を一度計算→共変Piolaで
物理要素へ→NGSolve HCurlへはZ行列で橋渡し。純内部bubbleではなく
**s界面のa_yトレースが要素間連続を担う**（= b_m(s)接続の在処）。

## 2026-08-17 CanonicalHCurl 追加裁定: B経由射影の確定・curve order・矩形断面

菅原裁定（確定、再審不要）:
1. **射影はB経由で確定**。理由の核心=「ゲージ変換」と「curl curl A=0求解」が
   B入力+gauge-rigid真空空間では**計算ステップとして両方消滅**する（ゲージは
   最初から入れない構造、真空は基底が恒等的に満足）。A値入力はソースの
   クーロンゲージとの整列(KKT)を復活させるので不採用。A側の情報は
   ゲージ不変汎関数（∮A·dl flux行・flux証明書）としてのみ使用可。
   gauge-rigid空間上でcurlは単射（∇χの居場所を全て削った）=Bで一意。
2. **A-RKは2本立て**: canonical A-RK（CanonicalHCurlのAを消費=構造的にgauged、
   LIE−A-RK=Lie打切りだけを分離）+ exact-source A-RK（rad.Fldコイル解析A+
   等価電流厳密A、ゲージ整列なし、機械変数比較で射影誤差を分離）。
   KKTゲージ機構は本番経路から引退。
3. **midplane-only推定は禁止（定量根拠）**: 真空拘束下ではmidplane By(x,0,s)の
   自由関数数がdim=p_x(p_s+1)と一致し「決まってしまう」のが罠。実測
   （C型中心スラブpx=8,ps=2,maxh=0.02）: midplane-onlyは自己残差2.6e-4 Tに
   見えて真のアパーチャー誤差3.8e-3 T=**15倍の誤差隠蔽**（full-volumeは
   自己残差=真誤差で正直）。off-plane データの第一の役割は精度でなく
   **証明書の成立**。
4. **新旧の本質差の序列**: 表現多項式クラスはほぼ同じ。#1構造=真空拘束
   （bubble化・次元崩し・Maxwellフィルタ）、#1保証=全体積データ（正直な残差）。
   その他: ゲージ構造化・b_m(s)直読ジェット・DOF数千→16-40。
5. **curve order = p_s（s方向のみ）、断面は素の矩形**（アパーチャー形状
   不要、異方curve orderの心配は消滅）: 横方向はフレームチャートでaffine厳密、
   曲率はr0(s)/e_x,e_y(s)/h(s)の要素内s多項式（次数p_s）にだけ入る。
   実装の中身= 真空作用素のh̃を要素内s多項式化（mul_linearの多項式版）。
   幾何は自前クラスなのでNGSolve等方Curve(p)に縛られない（Z行列橋のloftのみ
   標準Curve）。

（追記）フリンジ・スラブのmidplane-only対決（px=8, ps=3, maxh=0.02）:
増幅は**観測されず**（audit 3.94e-2 vs full-volume 4.04e-2 T、×1.0；中心×0.9）
— 低ノイズメッシュ+打切り誤差支配の領域では真空漸化式がmidplane情報を忠実に
off-planeへ運ぶ。一貫して出た実害は**誤差隠蔽**: midplane-onlyの自己残差は
真誤差を中心15×/フリンジ9×過小報告（full-volumeは自己残差=真誤差で正直）。
禁止の根拠は「証明書が成立しない」こと。増幅は高px・粗ソース側の潜在リスク
として記録のみ（今日のデータでは未発現、正直な負結果）。

## 2026-08-17 CanonicalHCurl 界面連続性: 段階付き契約とスプライン対応の実測確定

`probe_canonical_interface.py`（研究scratch）で界面条件数(rank)を実測(px=8):

- **a_y全+a_s全トレース一致は解析接続に退化**（界面rank=dim → 隣要素が
  完全決定、チェーン全体=1要素分の自由度。多項式全ジェット一致=一意接続
  なので当然）→ **却下**。
- **正解=段階付きL1契約**: a_yトレース（HCurl必須、シート電流/p_yキック除去）
  + **b_m値の継続のみ**（a_s主要項、位相キック除去）。y²以上のa_s微小
  ミスマッチは打切り誤差オーダーで許容。
- **スプライン対応が閉じた**（保留項目の解決）: ps=2+L1 → 2条件/多極 →
  チェーン自由度 **E+2/多極 = C¹二次スプライン**厳密一致;
  ps=3+L1 → 3条件/多極 → **E+3 = C²三次スプライン級**（∂s³結合条件がC²役）。
  **ps=1はチェーン退化**（大域一次に潰れる）→ チェーン生産はps≥2必須
  （フリンジの奇数次有効性とも整合）。p_y_truncは条件数に影響しない
  （契約は評価打切りと独立）。
- **実装3択**（どれも軽い）: (1)推奨=b_m(s)を最初からスプライン基底で
  持つ直パラメータ化（連続性・周期性とも構成的に自動、界面拘束消滅）,
  (2)大域巡回帯LS（E·dim~数百で自明）, (3)逐次+閉包1回補正
  （Sherman-Morrison型）。
- **円形リングのコホモロジー障害**（菅原指摘の周期化で実体化）:
  ∮A·dl=リンク磁束はゲージ不変で、centre gauge空間では表現不能
  （EarlyTimes政策のlongitudinal circulation obstruction）。処方=
  **リングあたり大域DOF 1本**（a_s定数モードを1本だけ復活、
  値=リンク磁束/周長）。

## 2026-08-17 CanonicalHCurl Stage 3: 本実装着地と実C型初検証

**本実装（新規ファイルのみ、codex衝突ゼロ）**:
- `src/radia/beam_canonical_hcurl.py`: 純NumPy。CanonicalHCurlElement
  （gauge-rigid + 曲率分母クリア作用素 + 到達可能集合フィルタ + 固定次元
  least-defect基底; h̃(ζ)=要素内s多項式 = curve order=p_s; 次元法則
  p_x(p_s+1)を構築時assert）、CanonicalHCurlChain（L1界面契約、
  全体積Bフィット=midplane-only拒否、正直残差・界面ジャンプ・欠損
  スペクトル証明書、Lie核向け共変(d+1,d+1)係数 lie_segment_arrays、
  A-RK向け解析勾配 vector_potential_and_gradient_frame）。
- `tests/test_beam_canonical_hcurl.py`: 11/11合格（曲率+多項式h̃込み
  次元法則 / 曲率チャート一様場7e-16 / 2D多極機械精度 / スプライン法則
  p_x(E+p_s) / ps=1チェーン拒否 / 界面ハード連続 / midplane拒否 /
  勾配vsFD / Lie係数契約）。
- MATLAB parity: beamCanonicalHCurl.m + manifest登録（python-fallback）。
- `validation_test/ffag_topopt/validation_canonical_hcurl_ctype.py`:
  実C型ランナー（fit証明書 + native LIE vs native canonical A-RK）。

**実C型ランナーで踏んだ2バグと解決**:
1. 要素ごとにhが異なると界面拘束の厳密kernelが崩壊（chain次元0）—
   要素と同じ打切り病理。チェーン側も**固定次元least-defect**
   （目標=スプライン法則 p_x(E+p_s)、E=1で要素法則と整合）に。
   界面欠損1.5e-4相対、フィット後の実ジャンプ a_y 7.7e-8 T·m。
2. H1ゲート違反2.5e-2の正体は**空フィット**（次元0でAs=0 → H1_x=|h̃|
   そのもの）。修正後 h̃=−signed_curvature のまま H1 max=1.75e-3
   （フィット誤差整合）— **符号規約 h̃=−signed_curvature を実測確定**。

**初検証数値（maxh=0.02、16要素、px=8、ps=2、64セグメントLIE）**:
fit rel 5.8e-2（フリンジ支配、正直比1.06）; LIE−A-RK = **2.1e-5**
（scale 0.5、同一多項式上=Lie打切り+midpoint s-staging の分離対）。
LIE構築1093s（パラメータ・ヤコビアン込み合成が重い — 検証用途は許容、
将来最適化）。24要素+振幅スイープの本計測をbackground実行中。

（追記）Stage 3 帰属判定: LIE−A-RK床の正体はH1残差ドリフト。
振幅スイープ（0.25/0.5/1.0 → 1.79/1.82/1.94e-5、比1.0–1.1）で
Lieジェット開口打切り（~2⁵/倍振幅のはず）は床下と確定。セグメント半減
（64→32）でも床不変（1.82→1.70e-5）でmidpoint s-stagingも棄却。残る帰属
= **H1残差ドリフト**: フリンジのフィット誤差（H1~2.7e-3）による
「追跡軌道≠フィット場の真軌道」を A-RK は積分し、斉次LIE写像は捨てる。
床はフィット品質に比例 → 改善レバーは**フリンジ適応要素分割**。
旧パイプライン同対（2.2e-4、開口支配）から11×改善し誤差構造も変化
（開口打切り支配 → フィット品質支配）。LIE構築コストはセグメント数に
超線形（32seg:309s / 64seg:1096s — パラメータ・ヤコビアン合成）。

## 2026-08-17 CanonicalHCurl 残項目の実装（適応分割・四ルート・リング閉包）

- **graded_breaks**（`radia.beam_canonical_hcurl`）: |dBy/ds| 軌道モニタの
  等分配則で要素境界を配置（strength=0 で一様に退化）。テストで
  バンプ位置の要素収縮をロック。ランナーに `--grade-fringe` 配線。
- **四ルート交差**（ランナー `--include-cross-routes`）:
  (i) LIE(chain) / (ii) canonical A-RK(chain) / (iii) **exact-source
  A-RK**（生の解析A: rad.Fldコイル+等価電流厳密CF、FD勾配、a_x≠0を
  正準機構がそのまま受容、ゲージ整列なし） / (iv) **native機械B-RK**
  （b_point直結・経路長パラメータ・出口平面イベント、A表現から完全独立）。
  出口平面の機械変数 (x, px_mech, y, py_mech) で全対比較 — 各ルートは
  自分のAで正準↔機械変換（ゲージ共変）。CF評価は網内限定なので
  exact-source用に評価コンテキストのtubeメッシュのみ構築。
- **リング閉包（構造レベル完成）**: `periodic=True` で閉じ界面をL1契約に
  追加、固定次元目標=**周期スプライン次元 px·E**（ps=2,3で検証）。
  コホモロジーDOFは `set_ring_circulation(リンク磁束)` の大域定数
  共変a_s 1本（場ゼロ・力ゼロ・∮A·dl観測のみ復元 — 定数はHの勾配に
  入らないことと整合）。テスト13/13。実リング場での検証は
  リング案件が立った時。

## 2026-08-17 CanonicalHCurl 残項目完了: 適応分割効果・四ルート交差・飢餓ガード

**サンプル飢餓事件**: graded初回実行で薄要素(1.33mm)にステーションが
当たらず無拘束DOFが暴走（a_yジャンプ5.13 T·m、監査1.6e6 T）— **正直監査が
8桁の異常値で即検出**（midplane隠蔽と同型の罠を防いだ）。恒久対策2層:
本体にfail-fastガード（要素サンプル数<次元でsample starvation拒否、
テストロック）+ ランナーは要素比例サンプリング（floor 4ステーション/要素）。

**適応分割の効果（strength 6、要素1.33..8.42mm）**: H1 3.08e-3→**7.94e-4**
(3.9×)、LIE−A-RK床 1.9e-5→**2.7–3.1e-6**(6×)、正直比0.92。床がH1と共に
落ちた=「床=H1ドリフト（フィット品質）」帰属の**予測的検証**。依然振幅
非依存でLie打切りはさらに下。

**四ルート交差表（scale 0.5、機械変数、gauged A-RKが既定=菅原裁定）**:
LIE vs chain A-RK 3.0e-6 / chain vs exact A-RK 2.9e-6（射影誤差の追跡影響）
/ chain vs B-RK 3.1e-6 / **exact A-RK vs B-RK 2.1e-7**（chain非依存の
ソースレベル三角形 — 厳密Aソース・共変規約・h̃符号・B-RK電荷符号を相互検証、
FD床3.6e-7整合）。誤差収支の層別: ソース機構2e-7 ≪ 射影3e-6（支配項）。
B-RK は nfev 533 と軽量（機械RK直結）。

**curl整合の質問への回答実装**: B評価器=Aの厳密多項式curl（曲率チャート
FD照合テスト<1e-7でロック）; 「curl A vs 空気中解析B」検証はBフィット
残差+独立監査そのもの（毎回実行）。用語整理: HDiv-MMMのHDivは**M**
（鉄上BDM1磁化）、空気中Bは解析積分場（真値）、「HDiv B-map」は任意の
射影診断で現行B-RK経路外。

canonical結果JSONをgraded+cross版に更新（メタデータ入り）。テスト15/15。

## 2026-08-17 LIE核 264×高速化: topopt随伴のopt-out (parameter_jacobians=False)

菅原質問「LIE map構築に時間? 高速化したい」「earlytimesでも掛かっていた?
実装が悪い?」への回答と処置:
- **旧EarlyTimes検証も同一核・同一コスト**（fourth_order_lie_map_from_
  hcurl_transverseが同じ私設核を64セグメントで包むだけ — 新規退行ではない）。
- **実装が悪いのではなく目的違いの機能を無条件計算**: 核はtopopt用の
  設計感度（全セグメント係数40×n_segパラメータの随伴）を写像テンソル
  合成に常時随行させていた。プロファイル実測（合成係数・ソルブ抜き）:
  89%がeinsum、うち過半が縮約経路の再計画; 熱点は_cross_fourth_third_
  tangent等の随伴合成（ヤコビアン列ループで_cross_fourth_thirdを
  32,160回）。ジェット構築は上位に現れず。
- **処置**: 私設核に `parameter_jacobians=True` kwarg追加（既定True=
  完全後方互換、topopt経路不変）。Falseで随伴を丸ごとスキップ（値経路は
  同一; *_jacobianは0幅）。**実測: 64seg 1096s→4.16s（264×）、
  スケーリングも超線形→線形化**（16/32/64segで1.23/2.21/4.16s）。
  テンソルはビット一致（tests/test_lie_kernel_parameter_jacobians.py
  がロック）。検証ランナーは既定False。
- これで検証1サイクルの支配コストはLIE(18分)からfit+場評価(~8分)へ移行。

## 2026-08-18 p_s直接消費LIE実装完了: 非自励ステージRK4（要素内s依存をHamiltonianへ）

菅原質問「p_s=2は中点だけ? s依存性もLIE mapに反映される?」→中点staging
（区分的反映）の説明後「着手してね」で実装:

- **核**（accelerator_lie_topopt.py、追加のみ・constant経路無傷=影響52
  テスト合格）: s多項式セグメント入力 (n_seg, k_s+1, d+1, d+1)（ζ∈[-1,1]、
  曲率も(n_seg,k_c+1)のζ多項式可）を受理し、**非自励RK4**（ステージ点で
  ジェット再評価: begin/mid/end、次ステップへend再利用）で流を積分。
  H1ゲートは全評価ステージで実施。parameter_jacobians=False必須
  （spoly随伴は将来）。`_fourth_order_rk4_step_stages` 追加。
- **chain**: `lie_element_spoly_arrays(degree)` — 要素のζ多項式
  (E, p_s+1, d+1, d+1) + 要素長 + 曲率多項式をそのまま核へ（1要素=
  1 Lieセグメント）。
- **テスト**（19本全合格）: k_s=0でconstant経路一致（atol 1e-14）/
  **4次収束**（step半減で誤差>10×減）/ midpoint単一ジェットはs依存を
  丸ごと落とす（>30×悪化）/ chainアダプタのζ点評価一致。
- **実機C型（graded 24要素）**: **24 element-spolyセグメント・9.7秒**で
  LIE−A-RK 3.11e-6（振幅非依存）= midpoint-64の3.0e-6と同等（両者H1床下）。
  H1ゲートは全ステージ測定で8.22e-4。**「1フリンジ要素=1セグメント」運用が
  実機で成立** — p_s依存が写像に直接入る新規経路が生産可能状態。
  ランナー `--lie-staging element-spoly`。

## 2026-08-18 多軌道三者試験と1軌道コストの解体（913s→329s→計測中）

菅原指摘「C型1つに10分はかかりすぎ」「そもそも1要素」への診断と処置:

**誤診2回の訂正込みの真因究明**: (誤1)呼び出しオーバーヘッド説 →
バッチ一本化でも遅く棄却。(誤2)直列説 → CPU/壁時計比7.7×で棄却
（評価器は既に並列）。**真因 = demag厳密核の演算量そのもの**
（1378解析tet原子×65µs·thread/点 = 11.5ms/点）+ **theta=0.05が
Barnes-Hut treeを完全に殺していた**（tree==direct 4e-15 = 遠方圧縮ゼロ。
ヨーク帰路の遠方原子は圧縮可能なのに）。

**処置スタック**（C++追加なし — 「GridFunction継承の高速版」は不要と
裁定: 本番3ルートはmesh(x,y,z)を通らない; 将来必要なら既存native係数
クラスへのEvaluatePointsバッチ追加が正道）:
1. `vim.FieldFromSolution(..., tree_options=)` 配管（theta等を上書き、
   オプション別キャッシュ）。実測 theta=0.5: **5.1×・誤差5.4e-4**
   （フィット床の1/100）。ランナーは500点プローブでtree vs directを
   ゲート化。B-RK・軌道追跡は厳密directを維持（独立ルートの純度）。
2. クラウドのy≥0半分化（基底パリティ厳密+対称化源なので下半分は
   情報ゼロ）。
3. チェーン評価のベクトル化（指数配列キャッシュ+単項式行列@基底行列 —
   A-RK rhs・fit設計行列・評価器すべて共通。バッテリー9.5→6.2s）。
4. 情報最小サンプル予算（対象は~200 DOF — 「1要素」の指摘どおり
   数千点で数倍過剰; 24st×40pt、要素floor 3）。

**物理の発見（再現2回）**: LIE−B-RK ≈ 6e-8 ≪ LIE−A-RK ≈ A-RK−B-RK
≈ 3e-6。**A-RKはフィットのH1残差（軌道上線形項=フィット人工物）を
忠実に積分して背負い、斉次写像のLIEはH1を構造的に捨てるため独立B真値と
50倍良く一致** — 斉次化ゲートがMaxwell/軌道整合フィルタとして機能する
証拠。写像そのものは既に6e-8級。

**多軌道ランナー** `validation_canonical_hcurl_multiorbit.py`: 平行入射
オフセット軌道族（この短い弱磁石では運動量族はサジッタ~0.1mmで空間縮退
するため）× |y|≤80%半ギャップ、1ソルブ共有で軌道ごとに完全三者試験。
track_reference_orbitへ後方互換のentrance_offset_m追加。

## 2026-08-18 多軌道三者試験の完成・生産仕様確定 (p_x,p_s)=(5,2)・十極打切り検証

**段階別タイマーが暴いた残り150s** = 軌道追跡181s（単点厳密評価は
点並列が効かず~90ms/点×rtol 2e-11の贅沢設定で~2000評価）。処置:
軌道は「基準曲線の定義」（全ルートが同一曲線を共有しH1ゲートが整合を
吸収）なのでtree+許容度適正化で181→~20s。B-RKはソース厳密のまま
積分許容のみ1e-8（比較スケール5e-6の1/100）。A-RKはベクトル化で
30→1.8s。**1軌道 84–101s**（支配項はB-RKの単点厳密評価 — 将来レバー
=C++原子並列で~12ms/点）。

**十極打切りの実測**: p_x=8→5でfit rel 9.2e-2→9.7e-2（+0.5pt、m≥6尾は
小さい）、LIE−B-RK 1.2e-7維持 → **p_x=5=十極で十分**（A次数5/B次数4 —
既存のHCurl5/HDiv4契約の物理的意味そのもの; LIE核のd≤5上限とも一致し
p=6拡張は不要に）。

**p_s判定（菅原の疑義「p_s=3は本当に意味があるか」→実測で降格**）:
同一シード5軌道バッテリーで(5,2)と(5,3)が全証明書数%以内の一致
（LIE−A-RK 1.0e-5/3.8e-6/5.6e-6/2.1e-6/1.1e-5 vs ほぼ同値）。
厚い±5mm単一スラブで測った奇数次優位はgradedチェーン（薄いフリンジ
要素）では実在しない — **既定(5,2)=15 DOF/要素**、ps=3は「厚いフリンジ
1要素」運用のオプションに格下げ。

**5軌道三者試験（±8/±4/0mm平行入射、|y|≤80%半ギャップ、1ソルブ共有）**:
全軌道で三者一致3e-7〜1e-5、磁極端±8mm（アパーチャ端が磁極20mmの2mm
手前）でも崖なしの緩やかな劣化~2-3×。菅原の当初の問い「複数design
orbitで三者試験できるか」に完全肯定の実測回答。

**midpoint vs spolyの本質（菅原の疑義への整理**）: 中点の役割が違う —
ps=0細分は「場の凍結」（交換子項を落とすモデル誤差、大域Δs²）、spolyは
「RK4求積節点で厳密多項式を評価」（積分誤差のみ、大域Δs⁴）。ただし
現在の床では両者同等（3.0e-6 vs 3.1e-6）が正直な実測。決定的な非等価は
写像でなく**空間側**: ps=0要素はa_y≡0で∂s²結合を表現できず中心断面
ですらrel 1.1e-2の床 — **ps≥2は場表現のための必然**。

## 2026-08-18 原EarlyTimes = Yoshida分割演算子（石氏（京大）2016資料、to_developers/ishi/beam_tracking.pdf）

菅原提供（Lineメッセージへの回答資料; 近大セミナーで使用）の要点。登場人物3名: 吉田克久氏（元三菱）・Yoshida氏（東京天文台、factorizationの由来）・石氏（京大、資料著者 y.ishi）— 「石井」は誤りで不在:
- 局所座標Hamiltonian（eBρ正規化、**(a_y,a_s)2成分・a_x無し** =
  CanonicalHCurlと同じ軸性ゲージ契約）、近軸展開（√→p²、hx打切り）、
  双極a_s^(0)は½h²x²へ繰込み。
- **s依存は拡張位相空間**（sを正準昇格、共役p_s、独立変数σ）。
- 分割 H = H1(x調和振動・厳密回転) + H2(½(p_y−a_y)² —
  **生成子f=−∫a_y dy′の共役変換で厳密ドリフト化**) + H3((1+hx)a_sキック・
  冪零厳密) + p_s。**Yoshida factorization**（対称合成
  e^{−σ/2:ps:}e^{−σ/4:H1:}e^{−σ/2:H3:}e^{−σ/4:H1:}e^{−σ:H2:}…、
  全因子M^TJM=J検証付き）。
- a_s多極展開 b0..b3 + フリンジ項 b0″/b1″/b0′h′ = 我々の生成y級数
  （真空漸化式）が自動で出す項と同一物。資料p.2の流れ図
  （場マップ→基準軌道→RO基準多極展開→LIE）は現行実装の構造そのもの —
  CanonicalHCurl=「RO基準多極展開」段の一般化、b_m(s)=b0..b3(s)の一般化。
- 相違: 石方式=逐次積分器（因子厳密・任意振幅シンプレクティック・
  誤差はσ分割2次のみ）/ 現行=打切り多項式写像（1写像使い回し・状態4次・
  Dragt–Finn因子化）。運動学は近軸 vs 厳密√次数6。
- **提案（未着手）**: Yoshida分割ルートを第4ルートとしてCanonicalHCurl
  多項式上に復活（全因子閉形式、f=−∫a_y dyも多項式; ~150行）。効用=
  系譜連続性・大振幅シンプレクティック追跡・**近軸近似誤差の初定量化**。

## 2026-08-18 第4ルート実装完了: Yoshida分割演算子をCanonicalHCurlで駆動

菅原指示「実装の場を可能な限りcanonicalHCurlで渡せる実装を」を実現。
`src/radia/beam_yoshida_split.py`（純NumPy）: 場の入力は**全て**chainの
多項式評価 — ãy・ãs^cov・h̃(s)（要素曲率多項式、新evaluator
curvature_frame）・H2厳密因子の生成子積分 G=∫ãy dη（新evaluator
ay_y_antiderivative_frame、閉形式y反微分）。

**構造は石氏deckの回文そのまま**: ps/2·H1/4·H3/2·H1/4·H2·H1/4·H3/2·
H1/4·ps/2（ps端キャップが内側因子を自動的にステップ中点sで評価）。
- H1=½px²+½h̃²x²: 厳密回転（sinc安全形、h̃→0でドリフトに退化）。
  純双極のãs^cov=−h̃x−½h̃²x²から½h̃²x²が厳密に出ることを確認して分割。
- H2=½(py−ãy)²: **v=py−ãy保存**による厳密解（yは線形ドリフト、
  px+=∂x[G(y1)−G(y0)]、py=v+ãy(y1)）= deckのf=−∫ãy dy′共役の閉形式。
- H3=U−½h̃²x²（U=−h̃x−ãs^cov）: 冪零厳密キック。
- order=4はYoshida係数 w1=1/(2−2^⅓), w0=1−2w1（負の中央ステップ）。
- psは純簿記（何もpsに依存しない）なので非追跡と明記 — deckの
  「hh′x²でぐちゃぐちゃ」問題は横力学に影響しないことの整理で解消。

**テスト5本**（25/25全体緑）: 大振幅（x=5mm,px=0.05）・大ステップでの
FDヤコビアンM^TJM=J<5e-7 / 双極平衡で軌道保持<1e-12 / 2次・4次収束
（>3×,>10×/半減）/ **近軸ギャップの初定量化**。

**近軸ギャップ測定**（釣り合い双極鎖・初期運動量P制御）:
- P=0で**5.6e-18=機械ゼロ**（落とした項は全てp²持ち）
- P倍化で比**4.000/4.000/4.001 — 厳密なp²則**
- 定量: **gap ≈ 2.9e-7×(P/10mrad)²**（80mm双極、P=20mradで1.2e-6）
- 教訓: 初回テストは非釣り合い合成鎖でp_curv~6e-3の場非依存床に埋もれ
  比1.26に見えた — 「ゼロ場≠ゼロ運動量」（曲がるフレーム自体がpを
  汲み上げる）の切り分けで根治。

**大振幅の答え**（菅原質問）: シンプレクティック性は振幅無制限（因子厳密）;
縛りは場の証明領域と近軸p²のみで、後者は上式で予算化可能。
役割分担確定: 小中振幅高スループット=一括Lie写像 / 大振幅・長期
位相空間構造=Yoshida分割。MATLAB parity: beamYoshidaSplit.m+manifest。

## 2026-08-18 近軸Yoshidaルートの削除（菅原裁定: 使わないと結論するなら落とす）

ガントリ+FFAGを視野に入れた採否結論: **既定は両機械とも一括Lie写像
（厳密√）の一本**。近軸Yoshida分割は (a)ガントリ=h̃xP²~1e-4/mで失格かつ
単通過で不要、(b)FFAG長期=exact-drift昇格なしでは系統蓄積で不十分、
(c)検証役=厳密√のA-RKが上位互換 — **近軸版は使わない**が確定。

使わない経路の残置はサイレント誤物理の温床（No Cruft / Discard PoC
政策の適用対象）なので、**蒸留済みの上で削除**（cd81bd0ddに全実装が
残り復元可能）:
- 削除: beam_yoshida_split.py / test_beam_yoshida_split.py /
  beamYoshidaSplit.m / manifest項 / chainのYoshida専用evaluator 2本
  （curvature_frame・ay_y_antiderivative_frame — 他に消費者なしの死API化
  のため同時剪定）。
- **残る教訓（蒸留内容）**: 近軸予算式 gap≈2.9e-7×(P/10mrad)²/80mm双極
  （P=0で機械ゼロ・厳密p²則4.000×3連）; 回文構造とps端キャップの中点評価;
  H2のv保存＋y反微分による厳密解; 「曲がるフレームではゼロ場≠ゼロ運動量」
  （p_curv~∫h̃ds）; psは純簿記。
- **復活条件を明記**: FFAGのDA/長期を本気でやる時に**exact-drift版**
  （√を厳密に解くドリフト+キック、PTC系標準）として作り直す — 近軸版の
  復元でなく上位形での新設。設計材料はmemory+cd81bd0dd。

## 2026-08-18 設計連鎖の定式化（菅原）: 出口ビーム状態→写像→磁場→磁極形状の感度連鎖

菅原の定式化: 加速器設計の目的関数は 1)出口ビーム状態 → 2)輸送行列/LIE
map → 3)それを実現する磁場分布 → 4)その磁場を作る磁極形状、と階層的に
変化し、各層の感度を連鎖すれば微係数が求まる。

実装側の対応（各リンクの持ち主が既に存在）:
1←2: apply_dragt_finn_map(return_jacobian)+写像テンソル。
2←3: Lie核のパラメータ・ヤコビアン（=topopt随伴、検証ではopt-out）。
3←chain DOF: lie_segment_arraysは係数の線形写像。
chain DOF←Bサンプル: 縮約LSの擬似逆（線形）。
B←M: HDivFieldEvaluatorはM係数に線形。
M←形状: 既存topopt随伴（isochronous_topopt/vfrac系）。
**中間3リンクが全線形**なので、RDT（f3/f4/f5係数）や出口状態を目的に
磁極形状を設計変数とする全微分が既存部品の合成で閉じる — DA-topoptの
プログラム骨格。着手は単点評価C++原子並列（高速化最終項目）の完了後。

（追記・菅原の局所性指摘）「改善対象の輸送行列の位置とその磁場は強相関、
しかし出口ビーム状態は全位置の輸送行列に依存」— 機構側の対応:
2↔3リンクは**ブロック対角**（M_kは自セグメントのb_m(s)のみ依存 =
CanonicalHCurl要素分割が局所性を座標化）、1←2リンクは**合成サンドイッチ**
∂z_out/∂(場_k) = J_{M_N}…J_{M_{k+1}}·∂M_k/∂場（下流輸送 = 生成子移送 =
位相進み重み）。**算法帰結: DA-topopt連鎖は随伴（逆向き）ファースト** —
出口双対を下流→上流に1掃引すれば全セグメント局所感度がO(n_seg)で一括。
前進モード（40·n_seg接線を全合成に引きずる=264×高速化で特定した超線形
コストの正体）は少数パラメータ検証用に格下げ。

## 2026-08-18 高速化完了: 3分計台帳（菅原訂正「構築と追跡を混ぜるな」に基づく）

**C++原子並列化**（rad_hdiv_field_evaluator.cpp、最終レバー実施）:
Evaluateにn_obs<スレッド数の分岐を追加し、点並列が遊ぶ小バッチでは
AddExact原子ディスパッチを塊分割ParallelFor（チャンク別補償和→直列縮約）。
実測 **90→16.7ms/点（5.4×）**、原子並列vs点並列の一致1.8e-16
（補償和の順序変更によるULP級、ヘッダに文書化）。EvaluateSerial（CF経路）
は不変。B-RK 27.6→7-8s、軌道追跡は厳密ソースに復帰して12-15s
（tree不要になった=純度回復コストゼロ、rtol 1e-8/max_step 10mm）。

**追跡側の穴と対処**: 単状態apply_dragt_finn_mapは738µs/粒子/磁石
（Pythonループ束縛）で「lie mapができれば多粒子は劇速」の約束に未達
→ **apply_dragt_finn_map_batch**（(N,6)アンサンブルの粒子軸einsum+
バッチimplicit-midpoint Newton）を追加。単状態と1e-13一致（テストロック）。
実測 **113µs/粒子**（6.5×、N=1e4..1e6でフラット）。µs級の「劇速」には
縮約連鎖のC++/MKL化が次レバー（Newton反復×次数5縮約で~2e5 flops/粒子
がnumpy帯域律速）— 未着手と明記。

**最終3分計**（5軌道スイープ55-59s/軌道、証明書同等以上、
中心LIE-BRK 2.97e-7）:
- ①写像構築: ~37s/軌道（軌道13+サンプル11.5+LIE核10+monitor 2）
  +ソルブ5s（全軌道共有）
- ②検証バッテリー(opt-in): ~21s（audit 11-13+A-RK 1.8+B-RK 7）
- ③追跡: 単状態738µs → **バッチ113µs/粒子**（新API）
出発点913s/軌道混然一体から、構築37s・検証21s・追跡113µs/粒子に分離。

## 2026-08-18 追跡「劇速」達成: バッチLie写像適用のC++化

numpy einsumバッチ(113µs/粒子)をC++核 rad_lie_map_batch.cpp に置換。
次数3/4/5生成子の勾配+ヘッセを後端添字の逐次縮約連鎖(deg5: 7776→1296→
216→36、勾配とヘッセが同一連鎖から出る)で評価し、implicit-midpoint
Newton(予測子・残差・6x6部分ピボットガウス消去・無限ノルム収束判定を
Python単状態参照と一字一句同型)を粒子ごとに実行、ngcore::ParallelForで
粒子並列(RegionTaskManager自己ラップ、16粒子以下は直列)。pybind結合
lie_apply_dragt_finn_batch(GIL解放)。f5なし(third-order)も対応。

**実測(LAB devループ、従来計測と同条件)**:
- 正しさ: バッチC++ vs 単状態Python max差 **1.7e-18**(ゲート1e-13の10^5倍下)
- スループット: **0.66-0.77µs/粒子**(N=1e4..1e6フラット)
- 100万粒子×1磁石 = **0.66s**(numpyバッチ113s→**170×**、単状態→**920×**)

3分計台帳の追跡行を更新: ①構築~37s/軌道 ②検証~21s ③追跡**0.7µs/粒子**。
「lie mapができれば多粒子計算は劇速」の約束をC++縮約で実現。numpyバッチ
PoC(_homogeneous_generator_gradient_hessian_batch+apply_homogeneous_
lie_generator_batch)はDiscard-PoCポリシーに従い削除(単状態Python経路は
検証参照として存続、テストがC++ vs Python交差検証)。third-order経路も
テストに追加。A-RK/B-RK整合は写像構築側の証明(5軌道スイープ)のまま不変。

## 2026-08-18 三者整合の振幅スイープ（design-orbit回り有限振幅の証明）

証明書の測定粒子は design orbit 回りの有限振幅
(x,x'',y,y'',delta) = scale*(1mm,1mrad,1mm,1mrad,1e-3)、全長80mm
フリンジ込み。中心軌道で scale=0.5/1.5/3.0 を実測（同一写像・同一フィット、
出口最大成分差 [m/rad]）:
- scale0.5: LIE-ARK 4.6e-6 / ARK-BRK 4.9e-6 / LIE-BRK 3.0e-7
- scale1.5: LIE-ARK 5.1e-6 / ARK-BRK 4.3e-6 / LIE-BRK 8.0e-7
- scale3.0: LIE-ARK 5.0e-6 / ARK-BRK 3.5e-6 / LIE-BRK 4.3e-6

読み: (1) LIE-ARK は振幅に対しフラット ~5e-6 = フィット場のH1界面
アーティファクト床（振幅打切りなら急増するはず）。均質写像がこの
アーティファクトを捨てるため LIE-BRK << LIE-ARK が小振幅で成立。
(2) 対真値 LIE-BRK は 3.0mm/3.0mrad（y方向でフィット領域±4mmの75%）
でも 4.3e-6 — 使用開口の大半で数µm/µrad級/通過。(3) ±8mm軌道の1e-5は
ポール端のフリンジフィット品質であって振幅起因ではない。
誤差の主部は射影（フィット）床で、E/p_s/サンプリングで下げられる
（+4mm軌道は3e-7を実証）。b_rk時間の揺れ(27s vs 11s)は適応RK+LAB混雑で
証明書には無関係。

## 2026-08-18 構築側高速化: 37→9.2s/軌道（5軌道バッテリー2.2分）

「高速化をガンガン」第2弾（追跡0.7µs/粒子に続き構築側）。3レバー:

1. **LIE核の多項式エンジンをベクトル化**（10.45→1.7s、6.2×）:
   ハミルトニアンジェットの多項式が指数タプル辞書+純Python二重ループ積
   だった（ジェネレータ式3100万回/構築）。次数≤5・6変数の462単項式基底+
   事前計算積表6188ペア（同一打ち切り）で bincount ベクトル化。
   parameter_jacobians=False では接線幅0で前進AD演算が完全消滅
   （with_parameter_tangentsフラグ）。einsum経路キャッシュ
   （_cached_einsum、初回のoptimize=True経路を再生=ビット同一）で
   経路再計画25379回/構築を排除。Lieバッテリー25+4合格（ビット同一
   テスト含む）。
2. **軌道トラッカーを中央面B_zスプライン化**（13→0.0s/軌道）:
   軌道ODEはz=0中央面のB_zしか使わない事実を突き、スイープごと1回の
   treeバッチ格子（101×31、13s共有）+RectBivariateSplineで
   ~700回×16.7msの逐次厳密評価を µs級に。実行内ゲート（回廊40点
   vs厳密、5.47e-4=tree誤差と同値、補間誤差は完全に埋没）。
   軌道は全ルート共有のCURVE DEFINITIONなのでµm移動はH1ゲートが
   吸収（注: fitのB入力全体積射影とは別物 — midplane禁止裁定は
   3D場再構築の話でここは中央面曲線の定義）。
3. **クラウド点数半減**（fit_cloud 11.2→5.6s、audit 10.8→5.6s）:
   40→20点/駅。3成分クラウドは~200 DOFに対し33×過剰サンプル維持。
   theta 0.8試験は実行内ゲートが2.08e-2>5e-3で正しく拒否（fail-fast
   実証）→theta 0.5本番維持。

**最終5軌道スイープ**: 24-26s/軌道均一（構築9.2+検証15）、共有21s
（solve 5.7+treeゲート+格子13）、全バッテリー**2.2分**。証明書は
既確立バンド内: LIE-BRK 3.1e-7..6.1e-6、honesty 0.83-0.99、
H1 1.3-1.9e-3。JSON再生成済み。今朝の55-59s/軌道から2.3×、
出発点913s/軌道から**~100×**。

## 2026-08-18 菅原裁定2件: トラッキングは3D（平面トラック禁止）+ 実装はC++

**裁定1「トラッキングは3Dだよ、平面トラックはだめ」**: 参照軌道トラッカー
track_reference_orbit は誕生時から [0,0,B_z] 射影+積分後の z=0/t_z=0 強制で
平面化されていた（中央面対称磁石では対称性により厳密だが、ガントリの
ロール磁石・スパイラルFFAG・据付誤差では破綻する仮定の焼き込み）。
本日のスプライン高速化はその既存仮定を加速しただけ → 裁定によりスプライン
ごと撤廃。修正: 全3成分・3Dローレンツ力で積分し、平面性は仮定でなく
**実測ゲート**（max|z|・max|t_z|·L vs planarity_tolerance_m=1e-6、超過時は
「平面フレーム機構は適用不可、Bishop枠3D軌道チェーンが必要」と fail-loud）。
粒子追跡側（LIE 6D/A-RK 6D/B-RK力学3D/C++バッチ適用）は元から完全3D。

**再証明（3Dトラッカー5軌道）**: 全軌道ゲート無音通過、証明書バンド内・
中心軌道 LIE-BRK **7.1e-8**（過去最良）。36-40s/軌道 =
①構築 ~22s（軌道13[3D厳密]+雲5.5+LIE核1.6+monitor 1.9）
②検証 ~15s ③追跡 0.7µs/粒子。バッテリー全体 ~3.2分。
構築が今朝の37s→22sに落ち着いた形（平面近道を返上して9.2→22s、
それでも出発点913sから41×）。

**裁定2「実装はC++、pythonはAPIを呼ぶ道具、MEX経由利用もかなり考えている」**:
既存の Python-to-MATLAB Capability Parity ポリシー（C++単一真実源+thin
pybind/MEXアダプタ）をbeam配管に適用。rad_lie_map_batch.cpp（pybind今、
standalone MEX ABI後）が確立パターン。C++移植バックログ: ジェット基底
エンジン（462基底+積表 — C++化容易）、RK4ステージ核+合成、チェーン評価、
フィットLS組立、軌道RK。本日のnumpyベクトル化は中間形であり最終形ではない。
