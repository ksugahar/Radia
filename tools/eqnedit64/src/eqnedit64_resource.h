#ifndef EQNEDIT64_RESOURCE_H
#define EQNEDIT64_RESOURCE_H

#define IDI_EQNEDIT64 101

/* Latin Modern Math, embedded as RCDATA.  The canvas has to draw with the
 * font TeX itself uses, and the executable has to stay one portable file, so
 * it travels inside the exe and is extracted to a verified per-user cache. */
#define IDR_MATH_FONT 200

#endif
