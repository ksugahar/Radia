# Nodal-force validation

`nodal_force_results.json` records the analytical, Maxwell-stress, and
virtual-work/nodal-force convergence evidence presented by
`docs/nodal_force/nodal_force.ipynb`.

The notebook owns the public explanation and embedded plot. This directory owns
the checked numerical record; docs do not write a result sidecar.
