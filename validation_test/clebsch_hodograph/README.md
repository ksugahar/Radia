# Clebsch/Hodograph validation evidence

This directory owns checked numerical records for the executed public notebooks
under `docs/clebsch_hodograph/`. The notebooks retain the explanation, figures,
and embedded output without writing adjacent JSON or checksum ledgers.

The existing executable golden checks remain under `validation_test/feec/`.
