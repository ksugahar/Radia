# WebGUI discovery expansion, 2026-09-15

Eight independently executed visualization scenes were inserted after the
opening cell of three public notebooks:

| Notebook | New views | Scope |
|---|---|---|
| `docs/electrostatics/electrostatics.ipynb` | Annular mesh, potential, electric-field vectors | Analytical infinite coax; not FEM/fringing evidence |
| `docs/background_fields/background_fields.ipynb` | Sampling mesh, source magnitude, source vectors | Prescribed quadrupole; not nonlinear iron response |
| `docs/gmsh_animation/gmsh_animation.ipynb` | Rotor and stator mesh | Existing `.vol` reloads; not registered assembly or dynamics |

Only the added supplemental cells ran on mdx2, using two NGSolve threads.
All historical code-cell objects (including outputs) were retained unchanged;
their hashes are checked by `tests/test_docs_webgui_supplements.py`. Original
runtime and new visualization runtime are separately recorded in sidecars.
Widget-view outputs and referenced scene states are saved in each notebook.
No new citation keys were introduced; the canonical 520-entry bibliography
resolves citations in all 53 audited notebooks with zero gaps.

The focused notebook, bibliography and saved-scene suite passed 26 tests.
Each updated notebook passes result audit (3, 3 and 2 rich WebGUI cells,
respectively; no error outputs). These are structural checks, not rendered
visual acceptance. Interactive viewing requires a WebGUI-capable viewer;
GitHub static previews may not activate widgets. Rendered visual QA remains
pending; the prior local-file browser restriction was not bypassed.

The numerical evidence and source/recovery receipt were committed in
`c39d56490`, without staging other pending docs or solver changes. All 17
recovered files matched their remote SHA-256 values. After that commit,
the exact mdx2 directory `C:/temp/radia_docs_webgui_20260915` was checked for
active task processes and reparse points, removed, and absence verified.
No shared Python environment or other job was modified.

Documentation maintenance owns the LAB scratch directories
`C:/temp/radia_docs_webgui_20260915` (prepared inputs) and
`C:/temp/radia_docs_webgui_recovered_20260915` (recovered scenes). Retain them
until permitted rendered visual QA and the final docs commit, then remove
these exact paths. The public notebook/sidecar changes, generator, regression
tests and MCP discovery descriptions remain uncommitted.
