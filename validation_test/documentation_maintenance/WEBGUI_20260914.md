# Cubit Mesh Export WebGUI follow-up

The public `docs/cubit_mesh_export/cubit_mesh_export_showcase.ipynb` now
contains three saved mesh scenes (geometry orders 1/2/3, 56 HEX each) and
one named manufactured Poisson field scene. All ten code cells were executed
on mdx2 with two NGSolve threads. Historical campaign tables remain clearly
separate from this new mesh reload/solve; Cubit itself was not rerun.

All three `check-vol` reports passed. Volume errors were approximately
-23.36%, -0.21%, +0.13%; the order-3 H1 solve used 1651 DOFs with L2 error
3.709016794121706e-7. Full precision, runtime versions and input hashes are
in `docs/cubit_mesh_export/cubit_mesh_webgui_results.json`.

The first run revealed a missing-anywidget text placeholder. A disposable
mdx2 environment was corrected and the complete notebook rerun. The saved
notebook contains four widget-view outputs and their referenced widget
states. The focused scene, bibliography and notebook-audit tests passed
(23 tests). These checks do not substitute for rendered visual review.

Browser policy blocked local HTML preview; no bypass was attempted. Final
visual QA remains pending in a permitted WebGUI-capable viewer. WEDGE and
multi-material examples are not covered by this update.

Numerical evidence and recovery/source receipt were committed as `68f685fe3`.
Remote and recovered hashes matched for the notebook, evidence and four HTML
scenes. After that commit, `C:/temp/radia_mesh_webgui_20260914` on mdx2 was
checked for task processes and reparse points, deleted, and absence verified.
No other job or shared Python installation was changed.

The LAB recovery at `C:/temp/radia_mesh_webgui_stage` is retained by the
documentation-maintenance task for pending rendered visual QA; remove it
after visual QA and the final docs commit. The docs, MCP discovery guidance
and new regression test changes remain uncommitted; the evidence-only commit
does not claim that the full docs maintenance is complete.
