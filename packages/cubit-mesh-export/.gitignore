# Pre-built binaries (copied by Build.ps1 at build time, bundled in wheel)
*.pyd
*.so
*.dll
*.ccm
*.ccl

# Build artifacts
dist/
build/
*.egg-info/
# Track the package's native command plugin.
!cubit_mesh_export.ccm
